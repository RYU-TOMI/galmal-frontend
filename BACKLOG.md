# BACKLOG.md — 프론트 곁가지 적재소

> **소유: 프론트 세션.**
> 작업 중 발견했지만 **지금 스코프가 아니라서 안 고친 것**을 여기 쌓는다. (`FRONTEND.md` §1 스코프 잠금)
> 항목마다 담당 챕터를 배정한다. 챕터가 시작되면 그 챕터의 태스크 목록으로 옮긴다.
> 해결되면 줄을 지우지 말고 `~~취소선~~ → 해결(커밋)` 로 남긴다.

## 열린 항목

> ⚠️ **번호 충돌: `B20`이 둘이다.** `:26`(핀 자체가 겹친다)과 아래 IA 절의 `B20 (IA-1)`(네비
> `노선별`이 링크가 아니다)이 같은 번호를 쓰고 있다. 인용할 때 뒤엣것은 반드시 `B20(IA-1)`로
> 적는다. 재번호는 이 파일을 다음에 만질 때. (2026-09-08 발견, 안 고침 — `FRONTEND.md` §8-4)

### CH1 — 지도 무대  ✅ 2026-09-02 완료
> 커밋 f200bcf · 472c7fc · 910fb5b · f342ae4 · 0e43c27 · a8fff05 · 1f5d371 · 87d07fc

- ~~**B1. 줌 컨트롤 ＋/－ 버튼이 죽어 있다.**~~ → **T2 해결**(`472c7fc`). 줌이 아니라 단계 스테퍼로. 양 끝에서 비활성, 버튼 안에 `더 멀리`/`가까이` 글자 + `aria-label`. 클래스 `.zoom` → `.stepper` 개명.
- ~~**B2. 단계 전환에 트윈이 없다.**~~ → **T6 해결**(`1f5d371`). 화면0→지도 600ms · 단계 400ms · `cubic-out` · 배율 로그 보간 · 트윈 중 라벨·항로 숨김 · 재클릭 시 현재 위치에서 재출발 · `prefers-reduced-motion` 스냅.
- ~~**B3 / B15. 라벨 밀집·잘림 규칙이 없다.**~~ → **T7 해결**(`87d07fc`). 4방향 후보(아래→위→오른쪽→왼쪽) + 무대 밖 배제 + 실패 시 라벨만 생략. **서울 아주 멀리 29쌍 → 0쌍.**
- ~~**B4. LOD 임계값.**~~ → **T3+T8 해결**(`910fb5b`, `0e43c27`). 배율 임계값 방식 폐기. 무대 안이면 전부 그리고 minor 를 5px·반투명·라벨 없음으로 낮춘다.
- ~~**B14. `아주 멀리`가 미주를 못 담는다.**~~ → **T4 해결**(`f342ae4`). 중심·배율을 그날 딜 경도에서 계산(최대 빈 구간의 정반대). 뉴욕·토론토·LA·밴쿠버·샌프란시스코·시애틀·호놀룰루 전부 복귀. 상수 없음, `resize` 반응.
- ~~**B17. 렌더 성능.**~~ → **T5 해결**(`a8fff05`). 세계 path 를 로드 시 1회만 만들고 뷰 변경은 `transform`. **브라우저 실측 22.04ms → 0.014ms(1633배).** 좌표 오차 1.14e-13px.

**남은 것**
- **B16. 필터 도크가 핀을 가린다.** `아주 멀리`에서는 T4의 배율 계산으로 해소됐지만 다른 단계는 그대로다. CH2에서 도크가 검색 한 줄(52px)로 교체되면 사라진다. → **CH2**
  - **라벨 쪽은 CH2 T7에서 닫았다** — 도크·줌 버튼 사각형을 라벨 배치에서 제외(무대 밖과 동일 처리).
  - **핀 쪽은 남아 있다.** 부산 출발 `아주 멀리`에서 **시드니·멜버른·오클랜드 3개 핀이 도크에 완전히 덮인다**(정적 계산, 무대 1171×807. 도크 높이를 280~460px로 바꿔도 3개 그대로 — 호주/뉴질랜드가 도크 한가운데에 온다). 피드엔 뜨는데 지도엔 없는 F15 상태다. 서울·대구·제주는 0개.
  - 라벨 제외의 실효는 오늘 데이터에서 **부산 `시드니` 1개뿐**(도크 밑에 그려지던 것이 이제 생략된다). 핀이 도크 밑이라 네 방향 후보가 전부 도크 안이라 옮길 자리가 없다 — **핀을 살려야 라벨도 산다.**
  - **처리 시점은 기획이 정해 뒀다**: SPEC §CH2 미결 B16 — *"PH5에서 도크가 하단 시트로 바뀔 때 함께 재검토"*. 지도를 줄여 피하는 건 대가가 크다(배율 상한 195→141). 프론트는 여기서 멈춘다.
- **B20. 핀 자체가 겹친다.** 홍콩·선전·마카오가 `아주 멀리`에서 1~2px 안에 스택된다. 라벨 규칙으로 못 푸는 별도 문제이고 스펙이 "긴급하지 않다"고 명시. → **후순위**
- ~~**B27. 제휴 고지가 뭉뚱그려져 있다.**~~ → **해결**(`f74c166`, 2026-09-02). `links[].ad` 가 참인 링크에만 `(광고)` 를 붙이고, 하단 문구를 `(광고) 표시는 예약하시면 저희가 수수료를 받는 링크예요 · 가격은 같아요` 로 교체했다.
  - **`ad` 필드를 백엔드에 요청해서 받았다.** 이름·순서·URL 모양으로 판정하는 건 넷 다 추측이고, 이 저장소가 반복해서 데인 패턴이다(`d=NRT` 가 공항이 아니었던 것, `MAX_AGE_DAYS` 가 일 단위가 아니었던 것).
  - 백엔드가 `ad` 를 Aviasales 하드코딩이 아니라 `_trip_configured()` 로도 계산한다 → **Trip.com 이 승인되면 프론트 코드 변경 없이 자동으로 `(광고)` 가 붙는다.** 내 요청보다 넓게 잡아준 것이다.
  - `가격은 같아요` 는 백엔드가 Travelpayouts 문서로 확인한 사실(수수료는 브랜드가 지불). 없으면 "그럼 내가 더 내나?" 가 남는다.
  - 검증: 실제 픽스처 127건 전부 — `(광고)` 표시 수 = `ad=true` 수 일치, 중립 3곳 0건, `tag` 노출 0건.
  - ⚠️ **브라우저 화면은 못 봤다**(확장 미연결). 사용자가 "광고 적당한 듯"으로 확인.
- ~~**B28. 확장 상세가 열린 채 정렬을 바꾸면 카드가 닫힌다.**~~ → **스펙대로 동작 중이다**(SPEC §CH4 「열고 닫기」 표: *정렬·필터 변경 | 닫는다. 지도는 그대로*). 고칠 게 아니었다. 다만 같은 표에서 **없던 것 둘**을 찾아 CH4 T6 에서 넣었다 — `×` 버튼과 「같은 것 다시 누르면 닫힘」. 그리고 정렬 변경 시 URL 이 상세로 남던 것도 T1 에서 `replaceState` 로 맞췄다.
- ~~(원문)~~ **상세 열린 채 정렬 변경 시 닫힘.** `collapse()`가 불린다. 스펙 §CH4 "열고닫기 10경우"에 정렬 변경이 포함돼 있으므로 그 규칙을 받아 처리한다. → **CH4**

### CH2 — 필터·정렬
- ~~**B5. 예산 슬라이더 상한(100만)이 실제 최고가(191만)보다 낮다.**~~ → **해결**(`da97992`, CH2 T3 · `c6f8e72`, T9). 트랙 min·max를 **현재 출발지 데이터**에서 5만 단위 올림으로 잡고, 최대치는 `제한 없음`으로 표기한다. 픽스처 121건 기준 서울 5만~130만 · 부산 10만~130만 · 대구 5만~125만 · 제주 25만~75만. 맨 왼쪽까지 내려도 최소 1건은 남는다(조작만으로 0건을 못 만든다). T9에서 바로가기 칩(`30만`·`50만`·`100만`·`상관없어`), T10에서 히스토그램(`987cad9`)을 얹었다. 눈금은 **선형** — 기획이 로그 결정을 재측정 후 철회했다.
  - (원문)  픽스처 기준 100만 초과 딜이 **15건**(2026-09-01 확정, 126건 기준. 최고가 **₩1,380,799**). 슬라이더 상한 100만보다 위에 15건이 있다. CH0에서 "필터 꺼져 있을 때 dim되는" 버그는 고쳤지만, **슬라이더로는 여전히 100만 초과 딜을 걸러낼 수만 있고 되살릴 수 없다.** 상한을 200만으로 올릴지, 최고가 기반 동적 상한으로 할지, 최대치를 "제한 없음"으로 표기할지 결정 필요.
- ~~**B6. 필터를 켜면 LOD가 무력화된다.**~~ → **해결**(`cff2a4f`, CH2 T4). 진짜 문제는 밀도가 아니라 **매칭이 화면 밖에 있는 것**이었다(F15). 필터를 켜면 `아주 멀리` 뷰로 전환하고(400ms 같은 모션), 라벨은 매칭에게만 준다. 실측: 5개 분위기 조건 전부 매칭이 화면 안 100% — `가까운 곳`에서는 21~30%였다.
  - (원문)  `visibleCities()`가 필터 시 `CITY` 전체를 반환해서 ＋유럽·미주 뷰에 minor 포함 전 도시(서울 기준 57곳)가 한꺼번에 찍힌다. "전 지역 매칭"은 확정 스펙이지만(`DESIGN.md`), 그 결과 지도 밀도가 무너지는 건 별개 문제.
- ~~**B26. 날짜 필터가 달력 주가 아니라 "오늘부터 N일"로 잰다.**~~ → **해결**(`ae91c1d`, CH2 T1).
  `dateWindow()`·`isoOf()` 제거. **날짜를 다시 계산하지 않고 `when` 값을 그대로 비교**한다.
  프론트가 따로 계산하는 한 백엔드 `when` 과 언제든 다시 갈라지므로, 계산 자체를 없앴다.
  - 고친 증상: 일요일에 `이번 주` → 정상 0건인데 18건(전부 다음 주). 토요일에 `이번 주말` → 오늘·내일 출발 10건이 사라지고 다음 주말 4건만 남음.
  - 칩 5종 → **8종**(`아무때`·`이번 주말`·`다음 주말`·`이번 주`·`이번 달`·`다음 달`·`그 이후`·`날짜 지정`). `이번 달`(48곳)·`그 이후`(28곳)가 없어 **127건 중 76건(60%)을 좁힐 수가 없었다.**
  - 칩에 건수 표시 + **0곳이면 흐리게·비활성**. 이미 고른 칩은 되돌릴 수 있게 남긴다.
  - 검증: 칩 6종 합계 = 127건 정확히 일치(빠짐·중복 0). 모든 칩에서 걸린 딜의 배지 = 칩 이름.
- ~~**B7. 필터 활성 상태 가시성.**~~ → **해결**(`e401ec9`, CH2 T5). 접힌 도크가 개수 대신 **조건**을 보여준다 — `필터 · 3` → `이번 주말 · 1~3박 외 2`. 최대 2개 + `외 N`, 순서는 도크 안 순서(날짜 → 며칠 → 분위기 → 예산). 8가지 조합으로 확인.

- ~~**C-10. 분위기 칩 어휘가 데이터와 안 맞는다.**~~ → **해결**(`e2d87bb`, CH2 T6). 칩 5종(해변·도시·온천·자연·미식) → **확정 6종**(`해변`·`도시`·`미식`·`자연`·`문화`·`온천`), 순서도 확정안(감성 → 실용)대로. 픽스처 121건 기준 커버리지 **121/121(100%)** — 어느 칩에도 안 걸리는 딜 0건. `문화` 53건은 `도시` 다음으로 많은데 그동안 고를 수가 없었다.
- ~~**C-13. 태그가 작은 카드의 세로를 먹는다.**~~ → **해결**(`d93f56c`, CH2 T8). 62px 썸네일 카드에서 태그를 빼고, 사진이 큰 자리(히어로 104px · 호버카드/확장 상세 92px)에만 **사진 위로** 얹었다. 표시 태그 = 하위 전부 + 상위 1개(하위 없으면 상위 2개), 최대 4개. 도시 69곳 중 68곳(99%)이 서로 다른 조합.
- ~~**B29. 도크가 길어지고 있다.**~~ → **모바일은 해소**(CH5 T2). 3단 시트로 바꾸면서 도크가 시트 위 접힌 알약 한 줄(370×47)이 됐고, full 단에서는 아예 치운다. **데스크톱은 그대로 약 570px** — PH5 에서 하단 시트로 갈 때 다시 본다.
- ~~(원문)~~ **도크가 길어지고 있다.** 예산 칩 줄과 히스토그램이 붙었다. **실측 약 570px**(1440×900 스크린샷에서 도크 상단 310px · 하단 882px). 앞서 적은 480px는 CSS로 어림한 값이라 90px 낮았다. 900px 화면에서도 안 넘치지만 여유가 생각보다 적다. 짧은 화면에서 도크가 무대를 넘칠 수 있다 — `max-height` + 내부 스크롤이 필요해질 시점을 봐야 한다. 지금은 안 넘치므로 안 고쳤다. → **CH5(모바일·하단 시트)와 함께**

- ~~**B30. 라벨이 다른 도시의 핀을 안 피한다.**~~ → **해결**(CH1 후속, 2026-09-03). 기획이 SPEC §CH1에 5단계를 넣었다(`52319b3`) — 피해야 할 사각형은 셋: 이미 놓인 라벨 · **다른 도시의 핀** · 도크/줌. 자기 핀은 뺀다(아래 후보가 자기 핀과 원래 겹치게 설계돼 있다).
  - 실측(무대 1171×807): **핀에 덮이던 라벨 15개 → 0개.** 대신 라벨 총량은 서울 22→16(도시 필터 20→12)로 줄었다. 기획 예상("생략률은 거의 안 오를 것")과 달랐다.
  - **읽히는 라벨로 세면 7개 → 16개다.** 덮여서 `이베이`로 읽히던 건 라벨이 아니다. 부산·대구·제주는 변화 0~+1(제주는 오히려 +1).
  - 스크린샷 확인: `베이징`·`서울 출발`·`삿포로`·`도쿄`·`상하이`·`오키나와`·`방콕`·`세부`·`괌` 전부 온전히 읽힌다.
- ~~(원문)~~ **라벨이 다른 도시의 핀을 안 피한다.** 라벨 배치는 **라벨끼리만** 충돌을 본다(`placeLabels`). 다른 도시의 핀(원)과 겹치는 건 검사하지 않는다. 필터를 켜면 매칭 41곳에 라벨이 다 붙어 밀집 지역에서 드러난다 — 스크린샷(서울 `50만 이하`) 실측: `타이베이`가 핀에 덮여 `이베이`로, `다낭`이 `낭`으로, `마닐라`가 `마닐`로 읽힌다. **CH1 T7 때 "29쌍 → 0쌍"으로 검증한 건 라벨-라벨 쌍뿐이었다.** → **CH1 후속 또는 CH3**
- ~~**B31. 출발지 라벨(`서울 출발`)이 충돌 검사 대상이 아니다.**~~ → **해결**(B30과 같은 커밋). 출발지 핀(ring r=9)과 라벨 사각형을 `placed`에 넣었다. 스크린샷에서 `베이징`과 안 겹친다.
- ~~(원문)~~ **출발지 라벨이 충돌 검사 대상이 아니다.** `og` 그룹에 있어 `placeLabels`의 `placed`에 안 들어간다. 필터 켠 화면에서 `베이징`과 겹친다. T7에서 도크·줌 버튼을 넣은 것과 같은 방식으로 넣으면 된다(사각형 하나 추가). → **CH1 후속**
- ~~**B32. 모바일에서 모든 카드가 사진이 큰데 태그는 히어로에만 붙는다.**~~ → **해결**(CH5 T4). 판정을 **사진 폭**으로 바꿨다. 실측이 규칙을 갈라 줬다 — 모바일 히어로 344 · 확장 상세 238/390 은 붙고, 작은 카드 썸네일 62 · **컴팩트 호버카드 177** 은 안 붙는다(컴팩트는 그동안 붙고 있었다). 개수는 히어로·확장 상세 4, 모바일 피드 카드 2.
- ~~(원문)~~ **모바일 카드 태그.** → 스펙 확정됨, 구현은 CH5. 기획이 판정 기준을 「카드 종류」에서 **「사진 폭」**으로 바꿨다(2026-09-03): **200px 미만 = 태그 없음 · 200px 이상 = 사진 위**(히어로·확장 상세·**모바일 피드 카드**). 개수는 폭에 따라 — 히어로·상세 최대 4개, **모바일 피드 카드 최대 2개**(스크롤로 지나가는 자리라 읽을 수 있는 만큼만). 200px인 이유는 태그 하나가 약 46px이라 둘이면 ~100px, 사진 폭의 절반을 넘으면 사진이 아니라 태그 판이 되기 때문. 카드 높이는 안 변한다. → **CH5(모바일)**

- ~~**B33. 단계바·프롬프트가 라벨 회피 대상이 아니다.**~~ → **해결**(2026-09-04). 기획이 규칙을 **이름 목록에서 성질로** 바꿨다(`7e2073c`) — *판정 = 그 사각형 아래에서 글자가 읽히는가.* `MAP_UI` 배열 하나로 모으고 숨겨진 요소는 자동으로 빠지게 했다. 실측: 단계바·프롬프트를 추가해도 **라벨 손실 0개**(서울 기본 화면), UI와 겹치는 라벨 **1개 → 0개**. 8방향 후보 완화책은 불필요.
- ~~(원문)~~ **단계바·프롬프트가 라벨 회피 대상이 아니다.** T7에서 `#fdock`·`#stepper`만 넣었는데, 지도 위 흰 박스는 넷이다 — `.stagebar`(`가까운 곳`·`조금 더 멀리`·`아주 멀리`)와 `.prompt`(`카드에 올리면…`)가 빠졌다. 스크린샷(서울 기본 화면) 왼쪽 아래에서 **라벨이 `가까운 곳` 버튼에 가려 잘린다.** SPEC §CH1 5단계는 "도크/줌 컨트롤"만 적고 있어 **기획 확인이 필요하다** — 같은 논리면 넷 다 들어가야 한다. → **CH1 후속 (기획 확인 후)**

- ~~**B34. hero 후보가 무대에 갇힌다.**~~ → **현행이 맞다**(기획 확정 2026-09-04, `6a3e074`). 거리 단계는 **뷰가 아니라 집합**을 바꾼다(`가까운 곳` = `haul: short`) → hero 도 그 집합에서 뽑는다. **정렬은 재배열이라 hero 고정, 단계·필터는 집합 교체라 다시 뽑는다.** 전체 기준으로 뽑으면 (a) 피드 목록에 없는 카드가 맨 위에 오고 (b) 핀이 무대 밖이면 F15 위반이다. 스펙 실측값(`대구 푸켓 31%`)에 **"전체 딜 기준"이 안 적혀 있던 것**이 혼동의 원인이었고 기획이 그 한 줄을 넣었다. 코드 변경 없음.
- ~~(원문)~~ **hero 후보가 무대에 갇힌다.** 현행은 `가까운 곳` 단계에서 **근거리 딜 중에서만** hero를 뽑는다. `SPEC.md:616`의 실측값(대구 `푸켓` 31% · 제주 `이스탄불` 11%)은 둘 다 중·장거리라 **스펙은 전체 딜 기준으로 쟀다.** 오늘 데이터로도 갈린다 — 대구 전체 `이스탄불 23%` ↔ 가까운 곳 `제주 18%`, 제주 전체 `하노이 37%` ↔ 가까운 곳 `상하이 16%`. 서울·부산은 우연히 같다.
  - 어느 쪽이 맞는지 스펙에 **명시가 없다.** 전체 기준이면 hero 핀이 지도에 없는 상태(F15)가 생기고, 무대 기준이면 "오늘의 발견"이 무대마다 바뀐다. **기획 확인 요청함.** → **CH3 (기획 답 대기)**

- **B35(신규). `.prompt`(`카드에 올리면…`)를 첫 방문에만 띄우고 닫을 수 있게 한다.** 기획 확정(2026-09-04) — T1의 첫 방문 띠와 같은 처리. **한 번 보면 되는 안내가 지도 자리를 영구히 먹는다**는 게 이유이고, 스펙이 이미 금지한 패턴("드롭다운을 미리 펼쳐 두지 않는다 — 작아진 화면0이고 딜을 가린다")과 같은 것이다. 닫히면 라벨 회피 사각형에서도 빠져 라벨이 그만큼 돌아온다.
  - 지금은 **회피 대상에 넣은 것만으로 라벨이 안 잘리므로** 급하지 않다. 기획도 "CH3 범위 밖이면 백로그에 넣고 넘어가도 된다"고 했다.
  - ⚠️ 기획 질문 하나가 열려 있다: **첫 방문에는 여전히 라벨을 가리는데 좌상단이 맞는 자리인가.** 자리 제안이 필요하면 프론트가 낸다. → **CH4 또는 CH5**

- ~~**B36. 390px에서 무대가 뷰포트보다 넓다.**~~ → **오진이었다**(2026-09-05 정정). 헤드리스 창에 **최소 폭(~500px)** 이 걸려 있어 `--window-size=390` 을 줘도 실제 뷰포트가 **489px** 였다. CDP 로 뷰포트를 직접 390px 로 지정해 다시 보니 **가로 넘침이 없다** — 헤더 알약도 온전하고 `body.scrollWidth == clientWidth` 다. 넘치는 것으로 보이던 212개 요소는 전부 `.feed` 안 카드인데, 그건 **가로 스크롤 피드의 정상 동작**이다.
  - 도구 함정을 `FRONTEND.md` 에 적었다. **화면 폭이 관련된 확인은 실제 `clientWidth` 를 같이 찍는다.**
  - ⚠️ 기획도 배포본에서 같은 증상(헤더 알약 잘림)을 보고했는데, **실기기에서 다시 봐야 한다** — 내 재현은 안 된다.
- ~~(원문)~~ **390px 에서 무대가 뷰포트보다 넓다.** 빈 날 화면(`.emptyday`)을 390×844로 찍어보니 노선 링크 칩 줄이 오른쪽으로 잘린다. **`.emptyday` 자체 문제가 아니다** — `max-width:min(560px,100%)`·`clamp()` 로 반응형 처리를 했는데도 잘리는 건 부모 `.stage` 가 이미 뷰포트를 넘기 때문이다. 900px 태블릿은 정상. 기획도 배포본에서 같은 증상을 보고했다(헤더 알약 잘림 · 지도 오른쪽 삐짐). → **CH5(모바일)**
- ~~**B37. 희소 안내의 숫자와 피드 헤더의 숫자가 안 맞아 보인다.**~~ → **해결**(기획 개정 `dba3aa7`). 안내를 **두 단계**로 나눴다 — 1순위 `더 멀리까지 보면 {M}곳이에요.`(무대 안 N < 전체 M), 2순위 기존 두 줄(전체 M이 10건 미만). **원인은 B34와 같았다**: 거리 단계가 집합을 바꾸는데 안내만 전체 기준을 썼다. 1순위가 먼저인 이유는 **거리를 넓히는 게 출발지를 바꾸는 것보다 가벼운 행동**이기 때문이고, `{M}`을 "더 멀리까지 보면"이라는 범위와 함께 말하므로 헤더와 달라야 정상이 된다.
- ~~(원문)~~ **희소 안내의 숫자와 헤더 숫자가 안 맞아 보인다.** 제주에서 헤더는 `제주 출발 · 3곳`(무대 안), 피드 끝 안내는 `제주 출발은 오늘 9곳이에요.`(출발지 전체)다. **둘 다 맞는 숫자인데 나란히 있으면 모순으로 읽힌다.** 스펙 F2의 판정선(10건 미만)과 실측값(대구 9건·제주 7건)이 **출발지 전체 기준**이라 안내는 그대로 두었다. 문구를 `가까운 곳에 3곳, 오늘 전체는 9곳` 식으로 바꿀지, 헤더 쪽을 손볼지는 기획 결정. **기획에 전달함.** → **CH3 (기획 답 대기)**

### CH3 — 카드 피드
- ~~**B8. 신선도 배지 — 계약 반영 완료, 프론트 구현 대기.**~~ → **해결**(CH3 T6, 2026-09-04). `seen` 이 128/128 전부 들어와 구현했다. 방문 시점에 브라우저가 계산하므로 페이지가 캐시돼도 알아서 나이를 먹는다. 실측(9/4 03:02 기준): fresh 2건(2%) · ok 34건(27%) · **stale 92건(72%)** · 없음 0건. 나이 최소 20.1h · 중앙 100.1h(4.2일) · 최대 181.5h(7.6일).
- ~~(원문)~~ **신선도 배지 대기.** 2026-08-22 백엔드가 `deals[].seen`(ISO8601, KST 오프셋 포함, 없으면 `null`)을 `CONTRACT.md`에 추가했다. 배지 문구·등급 계산은 **프론트 파생**이고, `null`이면 배지 대신 기존 폴백 문구를 쓴다. 시각 스펙은 `design/freshness.html`. **현 픽스처(`docs/data/deals.json`)에는 아직 `seen`이 없으므로**(103건 전부 부재), 구현 시 없는 경우를 먼저 처리할 것.
  - 필드명 주의: 내가 요청한 이름은 `found_at`이었으나 **확정된 이름은 `seen`**이다. 의미도 다르다 — 우리가 수집한 시각이 아니라 **소스가 그 가격을 마지막으로 관측한 시각**(=가격 자체의 나이).
  - 백엔드 일정: **BE2**에 배정됨(BE0 진행 중이라 즉시 아님). **`deals.json`에 실제로 나가기 시작하면 백엔드가 알려주기로 함** — 그 시점에 픽스처가 바뀐다.
  - 표기 규칙 3등급은 **`SPEC.md` §CH3**에 있다(`CONTRACT.md`가 `DESIGN.md`를 가리키는 건 재편 과정의 오기 — 기획에 전달함).

- ~~**B38. stale 경고 문구가 `사흘 전에`로 고정이다.**~~ → **해소**(COPY.md §2d 개정, 2026-09-04). 등급별 경고를 없애고 **확장 상세에 설명 한 줄**로 대체했다 — `항공권 가격은 예약 사이트가 마지막으로 조회한 값이라 며칠 전일 수 있어요`. 날짜를 안 세므로 `사흘` 고정 문제가 사라진다.
  - ⚠️ `COPY.md:443`(§3 미구현 목록)에 옛 문구가 **아직 남아 있다.** §2d 가 대체한 것으로 보고 지웠다 — 기획에 확인 요청함.
- ~~(원문)~~ **stale 경고가 `사흘 전에` 고정이다.** COPY.md 확정 문구가 `사흘 전에 본 가격이에요…` 인데 stale 은 **72h 초과 전부**다. 오늘 데이터엔 **7.6일(181.5h)** 짜리도 있어 6일 전 딜에도 `사흘 전에` 라고 말한다. **문구를 지어내지 않는 규칙** 때문에 확정본 그대로 넣었다. `{N}일 전에 본` 으로 바꿀지 기획 판단. → **기획 확인 요청함**
- ~~**B39. 확장 상세에 상대 표현이 두 번 나온다.**~~ → **해결**(기획 개정 `e694254`, 2026-09-05). 절대 시각 줄을 `2026-08-31 08:52 관측 · 3일 전 가격` → **`8/31(월) 08:52 기준`** 으로. **새 규칙이 아니라 날짜 줄의 두 단 위계를 그대로 적용한 것** — 위는 스캔용(상대), 아래는 확인용(절대), **각 줄이 한 가지만 말해야 둘 다 빨리 읽힌다.** 셋을 뺐다: 상대 표현(중복) · 연도(7일 안쪽이라 소음이고 `9/8(화)` 와 형식이 갈림) · `관측`(→ `기준`. `관측`도 **누가 했나**를 부른다).
- ~~(원문)~~ **확장 상세에 상대 표현이 두 번 나온다.** 가격 라벨 `발견가 · 3일 전 가격` 과 절대 시각 줄 `2026-08-31 08:52 관측 · 3일 전 가격`. 문구 개정 후에도 그대로다. COPY.md 가 절대 시각 형식을 `{…} 관측 · {상대 표현}` 으로 확정해 따랐는데, 가격 라벨 치환이 들어오면서 겹쳤다. → **기획 답 대기** 가격 라벨 `발견가 · 3일 전 확인` 과 절대 시각 줄 `2026-08-31 08:52 관측 · 3일 전 확인` 이 같은 카드에 있다. COPY.md 가 절대 시각 형식을 `{YYYY-MM-DD HH:MM} 관측 · {상대 표현}` 으로 확정해 그대로 따랐는데, 가격 라벨 치환이 함께 들어오면서 중복이 됐다. 절대 시각 줄에서 상대 표현을 뺄지 기획 판단. → **기획 확인 요청함**

### CH3 — 카드 피드 (추가)
- ~~**B24. 할인율 0인 딜에 `특가 0%↓` 도장이 붙는다.**~~ → **도장 부분 해결**(CH3 T4, 2026-09-04). `15/28/42` 3티어로 나누고 **15% 미만은 도장을 안 붙인다.** 픽스처 128건 기준 T3 4건 · T2 4건 · T1 19건 · **없음 101건(79%)**. `특가` 라는 단어도 뺐다(발견 홈 금지어, SPEC F19) — 문구는 `평소보다 {N}%↓`.
  - **`whyOf()` 훅 줄은 아직 남아 있다** — COPY.md 가 훅 줄 자체를 폐기했는데(2026-08-22) 코드에 그대로다. `경유로 확 싸진 특가`(근거 없음·금지어)가 여전히 붙고, `평소보다 N%↓` 가 도장과 **중복**된다. → **CH3 T5**
- ~~(원문)~~ **할인율 0인 딜에 도장이 붙는다.** 94건 중 **23건(24%)**의 `discount`가 0인데, `toCity()`가 `(dl.discount || 0) + "%↓"`로 무조건 문자열을 만들고 카드·플로팅 카드가 그대로 도장으로 출력한다. **"특가"라고 써놓고 할인율이 0%면 신뢰를 깎는다** — 우리가 파는 게 신뢰인데 화면이 스스로를 부정한다. 실제 화면 확인됨(후쿠오카·세부 카드).
  - 어떻게 할지는 스펙 문제다: 도장을 생략할지, 다른 문구(`최저가`·`평소 시세`)로 바꿀지, 애초에 `discount 0`인 딜을 피드 상단에 올리지 않을지. → **기획에 전달함(PH3).**
  - **2026-08-22 확대 실측 — 도장보다 훅이 더 나쁘다.** `whyOf()` 분기를 94건에 그대로 돌려봤다.

    | `whyOf()` 분기 | 배정 | 문제 |
    |---|---|---|
    | A. `평소보다 N%↓` | 4건 | 임계값이 `discount >= 25`인데 **프로젝트 특가 기준은 35%↓**(`config.py` 중앙값의 65%). 기준이 두 개다 |
    | **B. `경유로 확 싸진 특가`** | **51건 (54%)** | **아래 참조 — 가장 심각** |
    | C. `이번 주말 바로 출발` | 1건 | 사실상 안 뜬다 |
    | D. `최근 N%↓ · when` | 29건 | |
    | E. `when 최저가` | 9건 | |

    - **분기 B가 근거 없는 인과 주장이다.** `transfers > 0`이면 무조건 "경유로 **확 싸진**"이라고 단언하는데, **직항 대비 가격을 우리가 갖고 있지 않다.** `deals.json`에 직항 가격 필드가 없고 `median`도 직항/경유 구분이 없다(`CONTRACT.md`). 즉 **싸졌다는 주장을 뒷받침할 데이터가 아예 없다.**
    - 그중 **13건은 `discount`가 0**이다(발리·칭다오·홍콩·방콕·쿠알라룸푸르·호치민·광저우·멜버른·몰디브·자카르타·빈·시애틀·오클랜드). "확 싸졌다"고 말하면서 할인율은 0%.
    - **분기 순서 자체가 잘못됐다.** B가 D보다 앞이라, `discount 20%` + `경유 1회`인 딜은 **실제 근거(20%↓)를 버리고** 근거 없는 문구를 택한다.
    - 도장(`특가 0%↓`)은 수동적 과장이지만 **훅은 능동적 허위 주장**이라 더 나쁘다. 기획이 `SPEC.md` **F17** / `COPY.md` **C-11**로 등록했고, **PH3에서 규칙을 다시 준다. 그때까지 고치지 않는다.**
    - `discount` 분포(94건): `0%` 23 · `1~9%` 48 · `10~19%` 16 · `20~29%` 4 · `30%+` 3 → **10% 미만이 71건(76%)**. `35%+`는 **2건**뿐.
    - **2026-08-28 재실측(116건) — 전부 나빠졌다.**

      | | 94건 시점 | **116건 지금** |
      |---|---|---|
      | `discount = 0` (→ `특가 0%↓` 도장) | 23건 (24%) | **55건 (47%)** |
      | 분기 B `경유로 확 싸진 특가` | 51건 (54%) | **67건 (58%)** |
      | └ 그중 `discount = 0` | 13건 | **37건** |
      | 10% 미만 할인 | 71건 (76%) | 81건 (70%) |
      | `35%+` (프로젝트 특가 기준) | 2건 | **2건** |

      ⚠️ **위 116건 수치는 폐기됐다 — 확정치는 아래 "픽스처 확정" 절 참조**(`discount=0` 37건 29%, 분기 B 67건 53%). 방향은 그대로다: **카드 셋 중 하나가 근거 없는 도장을 달고, 22장이 할인율 0인데 "경유로 확 싸진 특가"라고 단언한다.**

      (이전 서술) 그리고 `경유로 확 싸진 특가`라고 단언하면서 할인율이 0인 카드가 **37장**이다. 새 데이터(도쿄·오사카·파리·런던·뉴욕)가 대부분 경유·저할인이라 비율이 올랐다. **F17~F19의 시급성이 올라갔다.**
  - 참고: `median <= price`인 딜도 21건 있어 확장 상세의 "평소 시세와 비교" 막대가 생략된다(`priceCompare()`가 `""` 반환). 이건 의도된 동작이지만, 도장은 0%로 뜨는데 근거 막대는 없는 **조합**이 생긴다.

### ✅ 픽스처 확정 (2026-09-01, 126건)
백엔드가 `BB10`·`BB11`을 고쳐 픽스처가 갱신됐다(`f8225c1`). **이전 116건 잠정치는 폐기하고 아래가 확정치다.**

- 무엇이 고쳐졌나: 신선도 계산이 UTC와 KST를 섞어 빼던 버그(9시간 부풀림) + `MAX_AGE_DAYS=3`이 실제로는 96시간에서 자르던 이름·동작 불일치(`MAX_AGE_HOURS=96`으로 통일). **정책 변경이 아니라 이름을 사실에 맞춘 것.**
- 확인: **도쿄 `SEL 287,179원` → `140,579원`**(거의 절반). 예상대로 잠정 픽스처의 값이 틀렸었다.
- 백엔드가 `collector/timeutil.py`로 "`found_at`은 UTC"를 아는 유일한 곳을 만들어 구조적으로 막았다. 같은 혼동이 세 번째였다.

| | 잠정(116) | **확정(126)** |
|---|---|---|
| `discount = 0` (F17) | 55건 (47%) | **37건 (29%)** |
| 분기 B `경유로 확 싸진 특가` (F18) | 67건 (58%) | **67건 (53%)** |
| └ 그중 `discount = 0` | 37건 | **22건** |
| 직항 / 경유 | 47 / 69 | **57 / 69** |
| 100만 초과 | 15건 | **15건** (최고가 ₩1,380,799) |
| `seen` 보유 | 116/116 | **126/126** |
| 허브 | 서울65·부산35·대구9·제주7 | **서울69·부산37·대구12·제주8** |

- **도장 티어 실측**: `T1(15~24%)` 17건(13%) · `T2(25~34%)` **3건(2%)** · `T3(35%+)` **5건(4%)** · 없음 101건(80%).
  ⚠️ **`T3`가 `T2`보다 흔하다** — 할인율 분포에 골짜기가 있다. 기획이 확인했고 **지금은 안 고친다**(하루 데이터에 맞추는 것이므로). 33일 이력에서 재현되면 그때 임계값을 옮긴다. **임계값 `15/25/35`와 시각 3단은 그대로라 CH3 구현에는 영향 없다.**
- 교훈은 유효하다: **픽스처 수치를 넘길 때 "언제 생성된 산출물인지"를 같이 확인한다.**

### 참조 — 우리가 실제로 아는 사실 (2026-08-22)
> 훅·카피를 만들 때 **이 표 밖의 것을 주장하지 않는다.** B24(F18)가 이 표를 안 보고 문구를 쓴 결과다.
> 기획이 예시로 든 `"3시간이면 도착"`도 여기 없어서 못 쓴다 — 전달함.

| 사실 | 출처 | 상태 |
|---|---|---|
| 직항 여부 | `transfers == 0` | ✅ 직항 41건 / 경유 53건. **현재 훅에서 전혀 안 쓰인다** |
| 경유 횟수 | `transfers` | ✅ |
| 거리(km) | `lat`/`lon` 대권거리 — 프론트가 계산 | ✅ 정확 (제주 451 · 후쿠오카 540 · 호치민 3,599) |
| 거리대 | `haul` | ✅ short 31 / mid 36 / long 27 |
| 출발일·기간 | `dep`·`ret`·`nights`·`when` | ✅ |
| 가격 나이 | `seen` | ✅ |
| 평소 시세 | `median` | ⚠️ 직항/경유 구분 없음 |
| **소요시간** | — | ❌ **필드 자체가 없음** |
| **직항 대비 가격** | — | ❌ **없음** (F18의 근원) |

- 거리에서 비행시간을 추정하지 않는다: 기종·항로·바람에 따라 다르고, **경유 53건은 대기시간이 지배적**이라 거리로 못 맞춘다. 추정을 사실처럼 말하는 순간 F18과 같은 문제가 된다.

### CH4 — 상세 전환
- **B9. 30일 가격 추이 스파크라인 부재.** `DESIGN.md`는 스파크라인을 명시하지만 가짜 데이터라 제거됨(`DECISIONS.md` 2026-08-06). 진짜 히스토리가 쌓이면 `CONTRACT.md`에 필드 추가 요청 → 그때 재구현. **그전까지 구현 금지.**
  - **2026-08-22 백엔드 실측 근거**: `offers` 테이블은 26개 노선만 깊게 수집하므로 **발견 딜 103건 중 히스토리 보유는 19건(18%)뿐**이다. 지금 구현하면 **82%가 빈 그래프**가 된다. 이 수치가 바뀌기 전에는 다시 열지 않는다.

### CH3 — 임박순 정렬이 연도를 무시한다 (2026-09-01 발견)
- **B25. `임박순` 정렬을 켜면 내년 출발이 1등으로 온다.** `discover.js:49`

  ```js
  function dkey(d) { var m = d.match(/(\d+)\/(\d+)/); return m ? (+m[1]) * 100 + (+m[2]) : 9999; }
  ```

  **표시용 문자열(`"9/12(토)~9/15(화)"`)에서 월·일만 뽑아 정렬한다. 연도가 없다.** 그래서 2027-01(→`111`)이 2026-09(→`901`)보다 앞선다.

  **실측(서울 69건, 임박순):**

  | 순위 | 도시 | 출발일 |
  |---|---|---|
  | 1 | 쿠알라룸푸르 | **2027-01-11** |
  | 2 | 나고야 | **2027-01-15** |
  | … | (내년 6건이 전부 위로) | |
  | 7 | 괌 | 2026-09-02 ← **실제 가장 임박** |

  - 전체 126건 중 **내년 출발 13건(10%)이 전부 앞으로 잘못 당겨진다.** 서울 기준 순서가 뒤집힌 쌍 **378/2346**.
  - **hero 동점 처리(`discover.js:132`)도 같은 `dkey`를 쓴다** — 할인율이 같으면 내년 딜이 hero로 뽑힌다.
  - **지금은 눈에 안 띈다.** `when`이 `"1월"`로 표시돼 올해로 읽히기 때문이다. **백엔드가 `내년 N월`을 도입하면 사용자가 "왜 내년 게 1등이지?"를 알아채게 된다** — 버그가 드러나는 것이지 생기는 게 아니다.
  - **원인은 파생값에서 다시 파싱한 것.** `toCity()`가 이미 `dep`(ISO 날짜)를 그대로 갖고 있는데(`dep: dl.dep`), 정렬은 굳이 화면 표시용으로 만든 문자열을 되파싱한다. **`dep` 문자열 비교면 끝난다**(`YYYY-MM-DD`는 사전순 = 시간순).
  - **2026-09-01 — 이제 화면에 그대로 드러난다.** 백엔드가 `when`에 `내년 N월`을 넣으면서(`6cd45b6`) 예상대로 가려져 있던 게 보이게 됐다. 서울 임박순 상위 5:

    | 순위 | 도시 | 화면 배지 | 실제 출발 |
    |---|---|---|---|
    | 1 | 쿠알라룸푸르 | **`내년 1월`** | 2027-01-11 |
    | 2 | 나고야 | **`내년 1월`** | 2027-01-15 |
    | 3 | 마쓰야마 | **`내년 2월`** | 2027-02-04 |
    | 4 | 마닐라 | **`내년 3월`** | 2027-03-12 |
    | 5 | 발리 | **`내년 4월`** | 2027-04-25 |

    실제 가장 임박한 딜은 **괌 2026-09-02**인데 아래에 있다. **"임박순"을 눌렀는데 상위 5개가 전부 `내년` 배지를 달고 있어, 사용자가 정렬이 고장난 걸 바로 알아본다.**
  - **고칠 곳은 두 줄이다.** `imminent` 비교자(`discover.js:87`)와 hero 동점 처리(`:132`)가 `dkey(c.date)` 대신 `c.dep`을 쓰면 된다. `dep`은 `toCity()`가 이미 들고 있고 `YYYY-MM-DD`라 문자열 비교가 곧 시간순이다. **스펙 의존이 없다** — 어떤 디자인 결정에도 안 흔들리는 순수 버그다.
  - 코드 변경이라 사용자 확인 필요. **CH3 태스크로.**

### 2026-09-01 12:26 픽스처 재측정 — `median` 재정의 반영
백엔드가 `median`을 `low`·`obs_days`와 같은 알갱이(`(허브,도시)`)·같은 기간으로 재정의했다(`45590b9`). 예고받은 `discount` 변동이 실제로 일어났다.

- **`discount` 평균 7.4% → 9.3%**
- **도장 티어: T1 17 · T2 6 · T3 8 · 없음 95** (이전 T1 17 · T2 3 · T3 5)
  - **`T3`(8건)가 `T2`(6건)보다 많은 역전이 유지된다.** 값이 바뀌었는데도 역전이 남았으므로 하루치 우연이 아닐 가능성이 올라갔다 → 기획에 전달함.
- **`median: null`이 14건 생겼다**(이력 없는 노선). **내 코드는 이미 견딘다** — `toCity()`가 `dl.median || 0`으로 받고 `priceCompare()`가 `if (!med || med <= now) return ""`로 막대를 생략한다. 실제 경로를 확인했다. **조치 불필요.**
- `when` 새 라벨 확인: `이번 달` 44 · `다음 달` 31 · `이번 주` 11 · `11월` 11 · `12월` 9 · `이번 주말` 7 · `내년 1~8월` 13. **이번 달·지난 달과 혼동될 라벨은 0건.**

### CH3/CH4 — `low`·`obs_days` 도착 (2026-09-01, 구현 대기)
백엔드가 `deals[].low`(이전 관측 기간 최저가, `null` 가능) · `obs_days`(관측 일수)를 실물로 내보내기 시작했다(`c7858eb`). `SPEC.md` §CH3의 **`{N}일 중 최저가예요`** 문구에 쓸 재료다.

- 실측(126건): `low` 있음 113 / `null` 13 · `obs_days` 중앙 **17일**
- 기획 확정 판정식 `obs_days>=7 AND price<low AND 하락 5%+` 재현 → **9건(7%)**. 백엔드 수치와 일치.
  - ⚠️ **이 줄은 낡았다(2026-09-20 표시).** `SPEC.md` §CH3 확정값은 **14일**이고 7일은 거기서 기각됐다. 이 줄을 보고 「발견 홈이 7일을 쓴다」고 기획에 잘못 전한 일이 있다 — **수치는 백로그가 아니라 SPEC 에서 확인한다.**
- **⚠️ `low`에는 오늘 값이 안 들어 있다.** 이번 수집 창(최근 3~4일)을 뺀 이전 기간의 최저가다. `price`가 이미 창 안의 최저가라 오늘 값을 넣으면 `price == low`가 41%가 되기 때문. 덕분에 `price < low`가 그대로 "신기록" 판정이 된다.
- **`obs_days < 7`이 33건(26%)** — 이 딜들은 문구를 아예 못 쓴다. `null` 13건과 별개다. **두 폴백 경로가 있다.**

**⚠️ 내가 발견한 겹침 — 기획 판단 필요(전달함).**
도장(`15%+`) 25건 · 신기록 9건인데 **둘 다인 카드가 6건**이다. 그 카드는 `평소보다 N%↓` 도장과 `{N}일 중 최저가예요` 문구를 **동시에** 단다.
- 둘은 **재는 대상이 다르다** — 도장은 평소 시세(`median`) 대비, 신기록은 이전 관측 최저가(`low`) 대비. 그래서 논리적으로 모순은 아니다.
- 다만 **한 카드에 "싸다"는 주장이 둘**이면 F17에서 정리한 방향(주장을 줄이고 근거 있는 것만)과 어긋나 보인다. 자리·우선순위를 기획이 정해야 한다.

**참고 — `median`과 알갱이가 다르다(지금 조치 불필요).** `low`·`obs_days`는 `(허브, 도시)` 단위인데 `median`은 아직 `(실공항, IATA)` 단위다. 백엔드가 BB4와 함께 정리 예정이며, **그때 `discount` 값이 조금 움직일 수 있다** → 도장 티어 분포가 바뀔 수 있으니 CH3 구현 후 재확인할 것.

### CH5 — 모바일
- **B10. 모바일에 컴팩트 카드 단계가 없다.** hover가 `mouseenter`라 터치에서 안 뜨고, 탭하면 곧장 확장 상세로 간다. `DESIGN.md`는 "모바일: 탭=표시, 바깥 탭=닫기"라 어긋나지만 실사용상 현재가 나을 수도 있음 → **기획 세션 확정 필요.**

### CH6 — 접근성·마감
- **B11. 키보드로 조작 불가.** 핀(`<g>`)·피드 카드(`<div>`)에 `click`만 있고 `tabindex`·`role`·키 핸들러·포커스 스타일이 없다. 마우스/터치 없이는 아무것도 못 함.
- **B12. 사진이 전부 태그 그라디언트 + "사진 준비중".** `DESIGN.md` 사진 정책(목적지당 1장 큐레이션, 호버 시 lazy-load, webp, 라이선스 표기) 미착수.
- **B13. `docs/index.html`이 278KB.** deals.json + world.geojson 인라인 때문. `file://`로도 열리게 한 의도적 선택이지만 초기 로딩 비용 측정 필요.

### 기획 세션이 프론트 구역에서 발견한 것 (2026-08-22, 스펙 대기)
> 기획이 코드를 고치지 않고 사실만 전달해 온 것. **스펙(`SPEC.md`)이 내려오기 전에는 고치지 않는다.**

- **B18 (F1, 최우선). 딜이 0건인 날 화면이 완전한 막다른 길이다.** `deals`가 비면 `origins`도 빈 객체가 되고, `renderIntro()`의 `Object.keys(D.origins).forEach`가 핀을 0개 그린다. **안내 문구조차 없다.** `DESIGN.md`에 있던 "오늘은 조용하네요"는 구현에 존재하지 않는다. 수집이 외부 API에 의존하므로 언제든 발생 가능. → 기획 PH3에서 문구·유도까지 묶어 스펙이 온다.
  - **2026-08-22 역할 분담 확정**: 백엔드가 `BB1`/`BE1`에서 **생성 쪽 방어**(수집 실패 시 이전 산출물 유지·하한선)를 맡는다. 따라서 **프론트가 만들 것은 "그래도 0건일 때의 화면"** 하나다. 양쪽에서 막는 구조이므로 프론트가 데이터 복구를 흉내 내지 않는다.
- **B19 (F4). `links`가 빈 배열이면 머리말만 남는다.** `compareHTML()`이 `""`를 반환하는데 호출부의 `"어디가 제일 싼지 비교해보세요"` 헤더는 그대로 출력된다. → PH4.
- **B20 (IA-1). 네비 `노선별`이 링크가 아니다.** `site/home.py:142`에서 `<span class="muted">`다. 노선 26개 링크가 `<noscript>` 안에만 있어 **JS 켜진 방문자는 도달할 수 없고**, SEO 내부 링크 신호도 버리고 있다. → PH6.
- **B21 (IA-2/F7). URL에 상태가 없다.** `location`·`history`·`hashchange`·`localStorage` 사용이 0건이라 새로고침하면 출발지가 날아가고, 특정 딜을 가리키는 링크를 만들 수 없다. 기획이 해시(`#SEL` / `#SEL-DAD`) 방향을 제안. → PH4.
- **B22 (C-2, 카피). `"이 조건엔 딜이 없어요"`의 "딜"은 내부 용어다.** `COPY.md` 금지어와 충돌. → PH3에서 문구가 내려온다. **`COPY.md`에 없는 문자열은 지어내지 않는다.**
  - **2026-08-22 전수 대조 결과**: 구현 문자열을 `COPY.md`와 기계적으로 대조했다. `COPY.md` §2는 현재 구현에서 긁어온 것이라 **거의 다 수록돼 있다**(표본 20건 중 18건). 실제 누락은 아래 3건뿐이며 전부 기획에 전달함.
    - `여행지 발견 지도` — 지도 SVG의 `aria-label`(`site/home.py:156`). **화면에 안 보여서 육안 점검으로는 안 잡힌다.** 스크린리더 사용자에게는 지도의 유일한 설명.
    - 예산 슬라이더 값 `100만` — `Math.round(budget/10000) + "만"`으로 **생성**된다. `만 이하`는 `COPY.md`에 있으나 숫자 생성 규칙은 없다. **B5(상한 재설계)와 함께 정해야 한다.**
    - ~~분위기 태그 — 필터 칩 5종으로 못 고르는 태그가 화면에 보인다(C-10).~~ → **2026-08-28 해결.** 계약이 어휘를 24종(**상위 6종 = 필터 칩** `해변`·`도시`·`미식`·`자연`·`문화`·`온천` + 하위 18종)으로 재설계하면서 구조적으로 풀렸다. 실측: **116/116건이 상위 6종 중 최소 하나를 갖는다**(칩 0개 매칭 딜 0건, 이전 4건). ~~다만 필터 칩 마크업은 아직 옛 5종이라 `문화`가 빠져 있다.~~ → **해결.** CH2 T6(`e2d87bb`)에서 6종이 됐다. 실측(2026-09-16): `site/home.py:60-65` 에 `해변`·`도시`·`미식`·`자연`·`문화`·`온천` 여섯 개가 다 있다. **고쳐진 뒤 백로그를 안 지워 미해결로 남아 있었다.**
      - 실측(`deals.json` **94건**, 2026-08-22 백엔드 BE0 이후): `도시`50 · `해변`35 · `미식`27 · `휴양`24 · `자연`21 · `쇼핑`20 · `문화`18 · `온천`6 · `액티비티`5 · `유적`4 · `야시장`2 · `트레킹`2.
      - **68/94건(72%)이 칩으로 못 고르는 태그를 달고 있고, 4건(이스탄불×3·델리)은 칩 5종이 하나도 안 붙어 분위기 필터를 켜면 어떤 조건으로도 매칭되지 않는다.** 딜 수가 103→94로 줄어도 비율은 오히려 올랐다(69%→72%).

- **B23. `TAG_GRAD`(14종)가 확정 어휘(24종)와 어긋난다.** `discover.js:17-25`. 2026-08-22 계약이 어휘를 **12종 → 24종**(상위 6 + 하위 18)으로 확장했다.
  - **없는 것 13종**: `고성`·`골목`·`길거리음식`·`리조트`·`마천루`·`미술관`·`사막`·`사원`·`서핑`·`섬`·`스노클링`·`폭포`·`화산` → 이 태그들은 **자기 색을 못 갖는다.**
  - **죽은 것 3종**: `설경`·`액티비티`·`휴양` (데이터 0건. `설경`은 계약이 "시즌 표현은 어휘에 넣지 않는다"로 **명시적으로 배제**).
  - **화면은 안 깨진다** — 실측 결과 기본 그라디언트로 폴백하는 딜 **0/116건**이다. 모든 딜이 상위 6종 중 하나를 갖고 그게 `TAG_GRAD`에 있기 때문. 다만 하위 18종은 색 결정에 관여하지 못한다.
  - 코드 변경이라 사용자 확인 필요. CH3에서 정리.

### 레포 분리 뒤 (M5·M6 이후) — 2026-09-17 적음

> 레포 분리(SPLIT.md M0~M6) 중에는 **동작을 바꾸지 않는다**는 규칙 때문에 미룬 것들이다.
> M6 이 끝나면 여기서 챕터로 옮긴다.

- ~~**B40. 🔴 `--no-snapshot-check` 는 픽스처를 새로 받는 날 지운다.** 영구 우회가 되면 안 된다.~~ → **해결(2026-09-19, vocab 챕터 T1).** 픽스처를 라이브 한 발행분(`16:04:43`, 40개)으로 다시 받아 적었고, 그 사본이 검사를 켠 채 통과해 플래그를 지웠다(`build.py` 인자 · `test.yml` · `CLAUDE.md`). `capture.py` 가 받아 적은 직후 같은 규칙으로 스스로 검사하게 해서 **섞인 사본이 다시 굳을 길도 막았다.**
  - 무엇: `site/build.py` 의 스냅숏 검사(받은 v1 39개가 한 발행분인가, M5 T6d)를 끄는 플래그다.
  - 왜 있나: 기준선 픽스처(`05d0de9`)가 이 규칙이 생기기 **전**의 발행이라 통과하지 않는다 —
    `deals` 만 42초 이르다(`meta 15:28:42` · `deals 15:28:00` · `preserved=false`).
    픽스처는 손으로 안 고치고, M2 동등성 증명의 짝이라 새로 받지도 않는다.
  - **지금 쓰는 곳은 딱 한 곳**: `.github/workflows/test.yml:40` 픽스처 빌드. 2026-09-17 전수 확인.
    배포(`deploy.yml`)·로컬 빌드에는 **번지면 안 된다** — 번지면 검사가 조용히 꺼진다.
  - **없앨 조건**: 픽스처를 라이브에서 다시 받아 적는 날(`python fixtures/capture.py https://api.galmal.kr/v1`).
    그 사본은 규칙을 통과하므로 플래그를 지우고 `test.yml` 픽스처 빌드에서도 검사를 켠다.
    M2 증명이 끝났으므로 픽스처와 기준선의 짝을 계속 유지할 이유는 M6 뒤엔 없다.

- **B41. 브라우저 탭 아이콘이 404** — `https://galmal.kr/favicon.ico`
  - 발견: M5 4번 직후 운영 화면 렌더에서 콘솔 에러 1건(2026-09-17). 응답 19개 중 유일한 400 이상.
  - **이전 회귀가 아니다** — 구형(`ryu-tomi.github.io/promo-ticket-site/favicon.ico`)도 404였다.
    `<link rel="icon">` 이 없어서 브라우저가 루트를 찍어본다.
  - 화면 영향 없음. 탭에 기본 아이콘이 뜨고 콘솔에 빨간 줄이 하나 남을 뿐이다.
    다만 **「콘솔 에러 0건」을 확인 기준으로 쓰는 이 프로젝트에선** 매번 걸러 봐야 하는 소음이다.
  - 아이콘 이미지는 기획(`DESIGN.md`) 소관. 프론트는 `public/` 에 두고 `shell.py`·`home.py` 에 링크를 건다.

- ~~**B42. 기획 문서 사본 3개를 프론트 저장소에서 뺀다** — `CONTRACT.md` · `COPY.md` · `TAGS.md` (R8)~~ → **해결(2026-09-17).** M6 뒤로 미뤘으나 M5 가 끝나 미룬 이유(스왑 중 원인 분리)가 사라졌고, **이미 하나가 낡아 있었다** — `CONTRACT.md` 사본 38,992B · 원본 41,184B. 기획이 오늘 원본만 고쳤다. T6d 를 짤 때 사본을 봤다면 **보존일 분기가 없는 옛 규칙**을 읽었을 것이다. 지우기 직전 읽는 코드 0곳 재확인.
  - 레포 분리 때 딸려왔다. 원본은 기획 쪽에 있어 **사본이 세 벌**이 됐다(기획 원본 · 백엔드 · 프론트).
  - 코드가 읽는 곳은 **0곳**이다(2026-09-16 전수: `open()`·워크플로 0건). 참조 13곳이 전부 **주석**이
    「이건 저 문서가 정본이다」를 가리킬 뿐이라, 사본 없이도 성립한다.
  - M5 스왑 중에 지우면 원인 분리가 흐려져 미뤘다. M6 뒤 지우고, 주석 경로를 `galmal-plan` 기준으로 고친다.

- **B43. `FRONTEND.md` 가 레포 분리 전 구조로 적혀 있다** — 2026-09-19 기획 지적, 내가 전수로 셌다
  - 옛 구조를 가리키는 곳 **25곳**(문서 1016줄): `docs/assets` 5 · `build_site.py` 11 · `discover_home` 4 ·
    `docs/index.html` 2 · `--directory docs` 1 등. 기획은 3곳(`:79` · `:946` · `:972`)을 짚었다.
  - 🔴 **두 종류라 한꺼번에 고치면 안 된다.**
    - **지금 따라 하면 틀리는 안내** — 예: `:79` `http.server --directory docs`(지금은 `dist`),
      §4 리빌드 규칙(`collector/build_site.py`·`docs/` 기준). **고친다.**
    - **그때는 맞았던 기록** — 예: `:946` 「크론은 아직 `site/build.py` 를 안 부른다」는 9월 초엔 사실이었다.
      §8 인수인계 절 대부분이 이쪽이다. **고치면 기록이 거짓이 된다** — 「이건 이전 전 기록」 표시만 한다.
  - 현재 규칙은 이미 `CLAUDE.md`(저장소판, `1ca1fb5`)에 맞게 적혀 있다. 그래서 급하지 않다 —
    다만 `FRONTEND.md` 를 먼저 펼친 사람은 옛 명령을 따라 할 수 있다.

- **B44. `TAG_GRAD` 에 어휘에 없는 태그 3개가 남아 있다** — `설경` · `휴양` · `액티비티` (2026-09-19 vocab 챕터 T3 중 발견, 안 고침)
  - 어휘가 24종으로 재설계될 때 빠진 태그로 보인다. 어떤 딜에도 안 쓰이므로 **지금은 해가 없다** —
    `grad()` 가 절대 그 키에 닿지 않는다.
  - 위험은 **다시 생길 때**다. 백엔드가 같은 이름을 다시 어휘에 넣으면 그 태그는 **예전에 정한 색을
    예고 없이** 받는다 — 디자인 결정이 조용히 부활한다.
  - T3 포괄 검사는 **어휘 → 매핑** 방향(빠진 것)만 본다. 이건 반대 방향(남은 것)이라 안 걸린다.
    지울지, 반대 방향도 검사할지는 기획(색은 `DESIGN.md` 소관)과 정한다.

### 중간점검 (2026-09-19, 레포 분리 기념) — 발견만 적고 **아무것도 안 고쳤다**

> 범위: 추적 파일 전부(`site/` 9 · `public/assets/discover.js|css` · 워크플로 2 · `fixtures/capture.py` · `tests/` 4)를 읽고,
> 테스트(33개 `OK`)·픽스처 빌드·헤드리스 크롬(1440/390, 홈·상세·노선)으로 쟀다. 라이브는 `build.json.api_generated == meta.generated`
> (`2026-09-19T16:04:43+09:00`) 일치, 최근 Actions 8회 전부 success, 콘솔 에러는 favicon 404(B41) 하나뿐.

#### ① 이 파일의 상태가 코드와 어긋난 것 — 위쪽에 **열린 채**인데 실제로는 끝난 8건

| 항목 | 코드의 증거 |
|---|---|
| B18 딜 0건 화면 | `discover.js` `emptyDay()` · `home.py` `#emptyday` |
| B19 links 0건 | `detailHTML()` 의 `hc-none` 분기 — 헤더째 생략 |
| B21 URL 상태 | `HASH_RE`·`parseHash`·`writeHash`·`applyHash` + `localStorage gm.origin` (단, 아래 B45~B47) |
| B22 「딜」 내부 용어 | 문구가 `이 조건엔 맞는 곳이 없어요` 로 바뀌어 있다 |
| B25 임박순 연도 무시 | `dkey()` 없음 — `depkey()` 가 `dep`(ISO)를 그대로 비교 |
| B10 모바일 컴팩트 단계 | `pinTap()` — 기획 확정(2026-09-05) 주석과 함께 구현 |
| B24 남은 절반(`whyOf` 훅) | 함수 자체가 없다. `transportHTML()` 로 대체 |
| B23 `TAG_GRAD` ↔ 어휘 | vocab 챕터 T3(`site/coverage.py`)이 빠진 쪽을 빌드 실패로 막고, 남은 쪽은 B44 |

**진짜 열려 있는 것은 11건**: B16 · B20(핀 겹침) · B20(IA-1, `home.py:179` 여전히 `<span class="muted">`) · B35(`.prompt` 데스크톱 상시 노출) ·
B9 · B11(실측: `tabindex` 달린 핀·카드 0개) · B12(`사진 준비중`) · B13(지금 `index.html` **353KB** — 적힌 278KB 보다 커졌다) · B41 · B43 · B44.
「다른 세션에 전달할 것」의 백엔드 줄(`seen` 이 픽스처에 없다)도 낡았다 — 지금 픽스처엔 있다.
위 항목들의 취소선 처리·B20 재번호는 **이 파일을 정리하는 태스크에서** 한다(이번엔 덧붙이기만 했다).

#### ② 새로 찾은 것

- ~~**B45. 🔴 딥링크가 거리와 무관하게 항상 「아주 멀리」로 열린다.**~~ → **해결**(CH7 T1 `a0d2c0b`, 2026-09-20). 라이브 실측: `#SEL-FUK` 가까운 곳·핀 25 / `#SEL-BKK` 조금 더 멀리·53 / `#SEL-LAX` 아주 멀리·79. `discover.js` `stageForHaul()`.
  - `toCity()` 가 `haul` 을 이미 단계 이름으로 바꿔 둔다(`short→near`). 그런데 `applyHash()` 가 그 값을 `stageForHaul(c.haul)` 에 다시 넣고,
    거기서 `HAUL2STAGE["near"]` 는 없으므로 `|| "far"` 로 떨어진다. **같은 매핑을 두 번 태운 것**이고, 폴백이 조용히 받아 줬다.
  - 실측(픽스처 빌드, 1440×900): `#SEL-FUK`(short) · `#SEL-BKK`(mid) · `#SEL-LAX`(long) **셋 다** 단계바 `아주 멀리` · 핀 79개.
    대조군 — 홈에서 후쿠오카 카드를 직접 누르면 `가까운 곳` · 핀 25개.
  - 공유 링크로 후쿠오카를 받은 사람은 유럽·미주까지 79곳이 찍힌 세계 지도에서 시작한다. `coverage.py` 가 막은 것과 **같은 폴백**이다 — 저쪽은 어휘→매핑만 본다.
- ~~**B46. 🔴 딥링크로 들어오면 상세 카드가 핀에서 떨어져 뜬다.**~~ → **해결**(CH7 T3 `f908814`). 첫 로드의 딥링크는 boot 재렌더 **뒤에**, 트윈 없이 연다. 라이브 실측 세 링크 모두 핀 [46,72]. **같은 분기를 쓰던 `prefers-reduced-motion` 도 같이 고쳐졌다** — 그쪽은 모든 상세 카드가 핀에서 떨어져 있었다(삿포로 가로 324px). `boot()` 의 rAF 재렌더(`discover.js` 마지막 줄)가 `expand()` 의 이동 트윈을 취소한다.
  - `render()` 첫 줄이 `tweenId++` 다. 딥링크는 `applyHash → expand → tweenTo` 로 지도를 핀 쪽으로 밀기 시작하는데, 바로 다음 프레임에 boot 의 재렌더가 돌아
    트윈을 무효화하고 뷰를 단계 기본값으로 되돌린다. 300ms 뒤 카드는 **핀이 도착했어야 할 자리**(가로 46% · 세로 72%)에 뜬다.
  - 실측: 핀 위치(무대 대비 %) `#SEL-FUK` [46,35] · `#SEL-BKK` [36,44] · `#SEL-LAX` [82,35] — 기대 [46,72]. 카드 클릭 대조군은 [46,72].
    **원인 분리**: 그 rAF 한 줄만 끈 사본으로 `#SEL-LAX` 를 열면 [46,71] 로 돌아온다. 후쿠오카는 핀이 카드 밑에 깔려 아예 안 보인다.
  - 그 재렌더는 라벨이 도크 밑에 깔리는 걸 막으려고 넣은 것이라(§3 QA 「첫 화면을 찍어 본다」) 그냥 지우면 그 문제가 돌아온다.
- ~~**B47. 🔴 안내의 `그 특가 보러 가기 →` 링크가 동작하지 않는다 · 앞으로 가기로 상세가 안 열린다.**~~ → **해결**(CH7 T2 `aa0b4d3`). `applyHash()` 가 이미 열린 딜은 다시 토글하지 않는다. 1차 때 분리 못 했던 「앞으로 가기」도 같은 원인이 맞았다 — 같은 수정으로 열린다. `popstate` 와 `hashchange` 를 **둘 다** 듣는다.
  - 앵커로 해시가 바뀌면 크롬은 두 이벤트를 다 쏜다 → `applyHash` 가 두 번 돈다 → 두 번째 `expand(i)` 가 「같은 걸 다시 누르면 닫힌다」에 걸려
    `closeByUser()` → `history.back()`. 주석은 「이미 반영된 상태면 안에서 걸러진다」고 적었는데 `expand` 가 토글이라 안 걸러진다.
  - 실측: `#CJU-LAX` 로 들어와 링크를 누르면 2.5초 뒤 주소 `#CJU-LAX` · 제주 출발 · 카드 없음 — **제자리**이고 안내까지 사라진다.
    `hashchange` 리스너 한 줄만 끈 사본에서는 `#SEL-LAX` · 서울 출발 · LA 상세 · 핀 [46,71] 로 정상.
  - 카드 열기 → 뒤로 → 앞으로: 원본은 주소가 `#SEL` 로 되돌아가고 상세가 닫힌 채다(실측). 같은 원인으로 보이나 **이쪽은 분리 확인을 못 했다**(측정 타이밍이 트윈과 겹침).
  - B45~B47 은 전부 「공유 링크로 들어오는 길」이다. `PRODUCT.md` 유입 경로(커뮤니티 시딩)가 이 길이라 셋을 **한 챕터**로 묶는 게 맞다.
- ~~**B58. 🔴 공유 링크로 들어온 사람이 `×` 를 누르면 사이트를 떠난다.**~~ → **해결**(CH7 T5 `6271894`). T2 를 재다가 발견. `closeByUser()` 가 앞 항목이 남의 페이지여도 `history.back()` 을 눌렀다.
  우리가 쌓은 히스토리 항목에 `{gm:1}` 표식을 달아 그때만 `back()` 한다. 라이브 실측: `#SEL-FUK` 진입 → `×` → `/#SEL` · 닫힘.
- ⏸ **PH5b 결론 뒤로 미룬다**(2026-09-21) — 아래 B59·B60 은 **모바일 시트·핀 위 카드**에 묶여 있고, 기획이 모바일 기본 화면을 다시 정하는 중이라 지금 공들이면 버려질 수 있다.
- **B59. 상세 A 를 연 채 B 를 열고 `×` 를 누르면 A 가 다시 열린다.** 실측: 후쿠오카 → 오사카 → `×` → 주소 `#SEL-FUK` · 후쿠오카 상세. `×` 가 `history.back()` 이고 앞 항목이 A 라서다.
  스펙은 「`×` — 카드만 닫힌다」(§CH4). 「동시에 하나만」을 히스토리에도 적용해 B 를 열 때 `replaceState` 로 갈지(그러면 뒤로가기가 A 를 건너뛴다)는 **기획 판단**이 필요하다. 안 고침.
- **B60. 상세를 연 채 창 크기를 바꾸면 카드가 핀에서 떨어진다.** 실측: 삿포로 상세 · 1440→1200 → 가로 **142px** 어긋남. `resize → render()` 가 뷰를 단계 기본값으로 되돌리는데 카드는 그대로다(B46 과 같은 뿌리 — `render()` 가 「밀어 둔 뷰」를 모른다).
  모바일 회전도 같은 길일 것으로 보이나 **안 쟀다.** 같은 뿌리의 다른 증상: 밀린 뒤에는 라벨이 재배치되지 않아 도크 밑에 깔린다(`#PUS-BKK` 7개 — 딥링크 아닌 카드 클릭 대조군도 똑같이 7개라 CH7 이 만든 게 아니다). 안 고침.
- ~~**B61. 데스크톱 상세에서 고지 설명줄이 카드 안 스크롤 아래에 있다.**~~ → **해결**(2026-09-20, 기획 결정대로 **순서**로 풀었다). `(광고)` 설명을 예약처 목록 위 캡션으로 — 1440·1366×768 에서 스크롤 없이 보인다. `.hc-seen` 도 12px·4.61:1. 대가: 1366×768 에서 스크롤 없이 보이는 예약처 4/5 → **3/5**. 테스트 12개가 잠근다. `DESIGN.md` 하한 표의 위치 조건 「스크롤해야 보이는 곳에 두지 않는다」와 어긋난다.
  - 실측(라이브 `#SEL-FUK`): 1440×900 · 1366×768 둘 다 설명줄 `visibleWithoutScroll=false`, 1366×768 은 **`(광고)` 표식 자체도** 안 보인다(Aviasales 가 다섯 번째 줄). 모바일 390 은 전부 보인다.
  - **B57 이 만든 게 아니다** — 고치기 전 라이브도 같았다. 다만 12px 가 되며 설명줄이 3줄 → 6줄(238px 카드)로 늘어 더 아래로 밀렸다.
  - 원인은 카드 높이 상한(핀 y − 16, SPEC §CH4)과 예약처 5곳. 설명줄을 목록 위로 올릴지 · 카드를 넓힐지 · 문구를 줄일지는 **배치 결정**이라 프론트가 정하지 않는다.
  - ✅ **기획 결정(2026-09-20, `DESIGN.md` §접근성 · `DECISIONS.md` 정정) — 반영함.** 「스크롤 없이 보인다」는 지킬 수 없는 규칙이었다(카드 높이는 지도가 정한다). 지킬 것은 **순서**다 —
    `(광고)` 표식은 링크에 붙어 다녀 누를 수 있는 순간엔 보이지만, **설명이 목록 아래라 뜻을 모른 채 누를 수 있다.**
    ① `(광고)` 설명 한 줄을 **예약처 목록 바로 위**로(목록의 캡션, 광고 링크 있을 때만, 문구 그대로) ② 가격 고지는 목록 아래 그대로
    ③ **`.hc-seen`(가격 기준 시각)도 하한 범위** — 12px · opacity 없음 ④ `.ovtag`·`.fr.stale` 은 고지가 아니라 CH6.
    기각: 카드 폭 확대 · 문구 축약 · 광고 링크를 위로(순서가 곧 추천).
    잠금: `test_disclosure.py` 에 `.hc-seen` 추가 + 「설명이 목록보다 앞」 순서 검사. 1366×768 스크린샷으로 목록이 얼마나 내려가는지 본다.
- **고지와 같은 패턴(작은 `--sub` + `opacity`)이 남은 곳** — 기획이 「이번엔 고지만」이라 해서 안 고쳤다: ~~`.hc-seen`~~(B61 에서 해결) · `.fr.stale`(opacity .7) · `.odopt i`(.9) · `.cmp-ad` 와 무관한 `.hc-photo .ph-tag`. 상세 카드에서 가장 작은 글자는 사진 위 태그 `.ovtag` **8.3px**.
- **B48. 테스트가 배포를 막지 않는다.** `deploy.yml` 과 `test.yml` 이 같은 push 에서 **따로** 돈다. 단위 테스트가 빨개도 사이트는 나간다.
  빌드 게이트 셋(스냅숏·매핑·칩)은 `deploy.yml` 안에 있어 그건 막힌다 — 안 막히는 건 `tests/` 33개가 지키는 것(차트 등)이다.
- **B49. 노선 페이지 CSS 의 69%가 안 쓰인다.** `shell.py` `CSS` 의 클래스 87개 중 **60개**가 노선 페이지 본문에 없다(`.card`·`.spark*`·`.tear`·`.gm-hdr`·`.gm-panel`·`#map .air-*` …
  옛 목록형 홈의 것). 12,252B 가 36장에 인라인 → 441KB. 같이: ~~`shell.logo()` 호출 0곳 · `home.py` `LOGO_SVG` 손 사본~~(B53 에서 해결) · `page(extra_script=)` 사용 0곳. `discover.css` 는 죽은 클래스 0/131, `discover.js` 는 안 불리는 함수 0/121.
- **B50. 코드 주석에도 레포 분리 전 서술이 남아 있다 — B43 의 코드판, 8개 파일 25줄.** 지시처럼 읽혀 따라 하면 틀리는 것만:
  `build.py:8` 「크론은 아직 옛 것을 부른다 — 스위치는 M3」 · `build.py --out` 기본값이 `docs`(문서는 전부 `dist`) · `shell.py:8` 「여기를 고치면 저기도 고쳐야 한다」 ·
  `route.py:8` 「이전 중에는 동작을 바꾸지 않는다」 · `charts.py:18` 「그때까지 두 벌」 · `deploy.yml:12,46` M4/M5 단계 안내 · `test.yml:3` 「`charts.py` 하나를 지킨다」(실제 4파일 33개) ·
  `snapshot.py`·`deploy.yml` 의 「39개」(지금 40). `route.py` `machine_date` 주석의 크론 관측 범위(상한 00:13Z)도 낡았다 — 백엔드 실측 00:31Z 까지, ~~예약 자체를 옮기는 안이 사용자 승인 대기 중~~ → **옮겼다**(백엔드 `2c3edba`, 예약 20:10Z). 새 슬롯 첫 실행은 22:18Z 시작(지연 2시간 8분, **1회 관측 — 범위로 적지 말 것**), 사이트 갱신 07:19 KST. 주석을 고칠 땐 「옛 슬롯의 기록」이라고만 표시한다 — `machine_date()` 의 방식(`generated` 를 UTC 로 바꿔 날짜를 뗌)은 그대로 맞다. `fixtures/capture.py` `_from_git` 은 `docs/v1` 이 없어 죽은 경로, `home.inline_deals()` 는 이전용 어댑터(§0-5 에 이미 적힘).
- **B51. 백엔드가 준 문자열을 이스케이프 없이 HTML 에 넣는 자리가 있다.** `discover.js` 의 `innerHTML`(도시명 `c.n` · `l.name` · `l.url` · `c.when` · `alt.ko`),
  `route.py` 의 `label`·`o_name`, `home.py` 의 `window.__DEALS=` 인라인(`shell.jsonld_block` 은 `</` 를 막는데 여긴 안 막는다). 출처가 우리 백엔드라 **지금은 해가 없다.**
  도시명·예약처 이름에 `<` 나 `</script>` 가 들어오는 날 화면이 깨진다 — `home.py` 의 나머지 자리는 전부 `html.escape` 를 하고 있어 **규칙은 있는데 빠진 자리**다.
- **B52. 작은 것 셋.** ① `snapshot.load()` 가 로컬 폴더에도 60초×12 재시도를 한다 — 픽스처가 어긋나면 12분 뒤에 실패한다. ② `capture.py` 가 받기 **전에** 옛 사본을 지운다 —
  받다 실패하면 픽스처가 빈다(git 으로 복구는 된다). ③ `resize` 리스너가 둘이라 모바일에서 리사이즈마다 `render()` 가 두 번 돈다.
- ~~**B53. 홈과 노선 페이지가 서로 다른 옷을 입고 있다.**~~ → **해결**(2026-09-20). 노선 다크 색표 제거 + `color-scheme:light` · 두 페이지 모두 `shell.logo()`. 홈 HTML 은 바이트 동일, 노선 36장은 CSS 와 로고 한 줄만 바뀜. `tests/test_shell.py` 8개(이전 코드에 돌리면 5개 실패). 실측(OS 다크 모드, 헤드리스 1440): 노선 페이지는 `prefers-color-scheme: dark` 에 반응해 **어둡게**, 홈은 **밝게** 뜬다 — `prefers-color-scheme` 은 `shell.py` 에 1곳, `discover.css` 에 0곳. 로고도 다르다: 홈은 코랄 SVG 워드마크, 노선은 청록 `말래` + `✈️` 이모지(`route.py` `.brand`). 홈에서 노선으로 넘어가면 다른 사이트처럼 보인다. 어느 쪽으로 맞출지는 `DESIGN.md`(기획) 소관. → **기획이 결정 거리로 받았다**(2026-09-19). `DESIGN.md` 에서 정해 알려 주기로 함 — 그때까지 프론트는 손대지 않는다.
  - ✅ **기획 결정(2026-09-20 (3), `DESIGN.md` §컬러·§로고) — 반영함.** 노선 페이지의 다크 색표와 `.brand` 로고는 레포 분리 전 `theme.py` 유물이다(`DESIGN.md` 는 다크를 **미착수**로 적고 있다).
    ① 노선 페이지 다크 매핑을 걷어낸다 — 전 페이지 라이트 하나, `color-scheme: light` 명시(폼 컨트롤이 다크로 안 가게) ② 노선 페이지도 `shell.logo()` — 청록 `말래`·✈️ 없이 홈과 같은 로고, 안 쓰이게 되는 `.brand` CSS 정리.
    끝났다의 기준: 다크 OS 에뮬레이션에서 홈·노선 둘 다 라이트 · 두 페이지 로고가 **같은 함수 출력** · 노선 36장은 로고·CSS 외 바이트 무변경. 기각: 홈에도 다크(지도 다크 매핑이 없어 반쪽이 된다).
- ~~**B54. 🔴 낮은 화면에서 필터 도크가 거리 단계 버튼(＋/－)을 덮는다.**~~ → **해결**(`6628f70`, 2026-09-20, SPEC §CH2 확정 A안). 도크 높이 상한 + 내부 스크롤 + **잘렸을 때만** 아래끝 페이드. 라이브 실측(폭 1366): 높이 768 버튼 26px 덮임 → 간격 +14 · 640 통째로 덮임 → 간격 +14·제목 무대 안·라벨 8→9 · 900 그대로(페이드 없음). 모바일 전후 동일. **핀이 도크 밑에 가리는 것(폭 900 에서 9/33)은 그대로다** — 그건 B16, PH5 재검토 대상. B29 가 「지금은 안 넘치므로 안 고쳤다」고 적어 둔 그 경우다 — 넘친다.
  - 실측(데스크톱 폭 1366, 펼친 도크 높이 **581px** 고정): 뷰포트 높이 768 → 도크 위끝 y=169, 스테퍼는 y=105~195 라 **아래 버튼 `가까이` 가 26px 덮인다.**
    높이 640(1366×768 노트북에서 브라우저 틀을 뺀 실제 높이대) → 도크 위끝 y=41 로 **무대(y=89)를 뚫고 첫 방문 띠까지 올라가** 스테퍼가 통째로 안 보이고 `필터` 제목도 잘린다. 1440×900 은 정상(y=301).
  - 도크에 `max-height` 가 없다(`discover.css` `.filterdock`). 모바일은 CH5 에서 하단 시트로 풀었고 데스크톱만 남았다. 접으면 해소되지만 **데스크톱은 펼친 채 시작한다.**
  - 같은 뿌리(B16)의 다른 수치: 폭 900 태블릿 `가까운 곳` 에서 **핀 33개 중 9개(27%)가 도크 밑**이다 — 오사카·나고야·도쿄가 통째로 가린다. 1440 은 0개.
- ~~**B55. 전선 규약을 만드는 코드에 테스트가 없다.**~~ → **해결**(2026-09-20). `tests/test_route.py` 30개(전체 53 → 83) — 구독 `mailto:`(픽스처 노선 전부가 자기 코드로 파싱되는가) · `machine_date` · 임계(`usable`·`months_shown`·`airlines_shown`) · 문장↔차트 일치(36장) · `weekday_name` 0=일 · sitemap. **일부러 망가뜨린 사본 7가지를 전부 잡는다**(`generated[:10]` · `usable>=1` · 0=월 규약 · 본문에서 코드 누락 · `n>=3` 누락 · sitemap 노선 누락 · mailto 미인코딩). `site/` 코드는 안 고쳤다. `tests/` 33개는 `snapshot`·`coverage`·`home` 칩·`charts` 넷만 지킨다(읽어 보니 넷 다 탐침·대조군이 있어 조용히 통과할 구석은 못 찾았다).
  안 지키는 것: `route.subscribe_link()`(형식이 어긋나면 **「전 노선 구독」이 된다**고 주석이 경고하는 그 자리) · `route.machine_date()`(UTC/KST 로 **실제로 틀렸던** 함수) ·
  `route.usable/months_shown/airlines_shown`(임계) · `fmt.weekday_name()`(0=일 규약) · `seo.sitemap()`. 지금 산출물은 맞다 — 36장 전수로 임계·JSON-LD 36개·내부 링크 41종·sitemap 37건을 대조해 불일치 0. **고칠 때 지켜 줄 것이 없다**는 얘기다.
- **B63. B55 를 쓰다 찾은 「전제에 기댄 코드」 둘 — 안 고침.**
  - `route.months_shown()` 은 **정렬하지 않고** 앞에서 10개를 자른다. 주석은 「월 오름차순 앞에서 10개」인데 오름차순은 **백엔드가 그렇게 준다는 전제**다. `fmt.weekday_name()` 은 같은 이유로 「배열 순서를 믿지 않는다」고 적어 놓고 값으로 읽는데, 월은 순서를 믿는다.
    전제가 깨지면 엉뚱한 달 10개가 조용히 실린다. 오늘은 성립한다(픽스처 36노선 전부 오름차순 — 테스트가 이 전제를 본다).
  - 구독 메일의 노선을 **백엔드 파서가 어떤 정규식으로 읽는지는 계약에 없다.** `meta.subscribe` 는 주소·제목·`route_token` 만 준다. 그래서 `tests/test_route.py` 에 백엔드 `ROUTE_RE` 의 **사본**을 둘 수밖에 없었다 —
    백엔드가 파서를 바꾸면 이 테스트는 초록인데 구독이 「전 노선」으로 떨어진다. 계약에 넣을지는 기획·백엔드 몫(전달함).
    → 백엔드가 `ROUTE_RE` 위에 「바꾸기 전에 프론트에 먼저 알린다」 주석을 넣었다(`64b83f4`). **반대 방향도 있다**: 백엔드 `tests/test_subscriptions.py::test_route_in_body_is_read` 가 우리 본문 모양(`노선: ICN-FUK (인천 → 후쿠오카)`)을 `site/route.py` `subscribe_link()` 에서 **보고 옮겨 적은 사본**으로 들고 있다. **본문 모양을 바꿀 땐 백엔드에 먼저 알린다.** `subscribe_link()` 주석에 이 한 줄을 넣는 건 다음에 그 파일을 만질 때(지금은 테스트만 만지는 챕터라 안 넣었다).
- ~~**B64. 새 노선의 첫날 페이지는 「데이터가 아직 충분하지 않습니다」가 세 번 나오는 거의 빈 페이지다.**~~ → **해결**(2026-09-20, 기획 결정 (7)). 수집 **14일** 미만 노선은 sitemap 에서 빼고(페이지·링크는 유지), 머리말이 실제 수집 일수를 말하고, 얇으면 「평소 시세」 칸을 빼고, 빈 안내는 하나로. 하한 14 는 세 곳이 같은 숫자(`route.MIN_DAYS`). **부산 노선 9장·타이중도 그동안 「최근 30일」이라고 틀리게 말하고 있었다**(19·18일째) — 같이 고쳐졌다. 남은 것: 공유 문구·메타 설명·항공사 리드·푸터의 「30일」은 COPY 에 D 판이 없어 그대로(기획에 물음). 백엔드가 노선을 36 → 43 으로 늘렸다(`b799bf1`, 2026-09-20 밤 크론부터).
  - 미리 구워 봤다(내일 응답을 흉내 낸 픽스처 사본 — 추이 1점 · 월 버킷 1개 · 요일 버킷 1개): **빌드 성공 · 43장 · sitemap 44 · 가로 넘침 0 · 예외 0.** 코드·테스트에 박힌 「36」「40」은 없다(주석·문서뿐).
    임계가 제대로 걸려 **거짓 주장은 0**이다(「가장 저렴합니다」 없음, `데이터가 쌓이면…` 으로 대체). `TAE-CJU` 빵부스러기는 `국내`.
  - 남는 것: 차트 자리 셋이 전부 같은 안내 상자이고, 「평소 시세(중앙값)」이 최저가와 **같은 숫자**다(하루치라서). 「최근 30일 수집한 가격 23건으로 분석한」이라는 머리말도 하루치엔 과하다.
    그 7장이 **sitemap 에 실려 검색엔진에 나간다.** 표본이 쌓일 때까지 어떻게 보일지(안내 하나로 접기 · sitemap 에서 빼기 · 그대로)는 문구·IA 결정이라 프론트가 안 정한다.
- ~~**B66. 🔴 새 노선 첫날 페이지가 「표본이 아직 얇습니다」라고 말한 직후 「가장 저렴합니다」라고 단정한다.**~~ → **해결**(`d726efc`, 2026-09-21, 기획 A안). `D`<14 면 팁을 안 쓰고 `데이터가 쌓이면…` 으로 — **차트는 그대로**(막대는 사실, 팁은 권고). 팁이 가리키는 칸에 `n>=3` 도 같이(팁에만, 요일 막대엔 안 건다). 라이브 7장이 실제로 그랬던 것을 픽스처 재수신으로 재현해 고쳤다. B64 를 확인할 때 쓴 **내 미리보기 가정이 틀렸다.**
  - 나는 첫날을 「추이 1점 · 월 버킷 1 · 요일 버킷 1」로 가정해 구웠다. 백엔드가 발행 규칙 그대로 재 준 실제 모양은 **추이만 1점이고 월·요일은 첫날부터 찬다** —
    `months`·`weekdays` 는 수집일 창이 없는 집계(「지금 팔리는 표의 출발 월/요일별 최저가」)다. 새 7노선 전부 월 n≥3 버킷 2~6개 · 요일 **7칸**(PUS-HAN 요일별 n = [1,1,1,2,1,3,2]).
  - 그 모양으로 다시 구우면: 머리말 `수집 1일째 — … 표본이 아직 얇습니다.` 아래에 `9월 출발이 가장 저렴합니다 · 출발 요일은 월요일이 가장 쌉니다` — **그 요일의 표본은 1건.** 안내 상자는 추이 자리 하나, 월·요일 차트는 선다(T4 의 「셋 다 빈」 경우가 아니다).
  - `usable()` 은 「비교가 성립하나」(버킷 2개)만 보고 「며칠 모았나」는 안 본다. 하루치는 가장 싼 100건의 단면이라 편향이 크다(백엔드).
  - 제안한 것: D < 14 면 팁 문장을 `데이터가 쌓이면…`(COPY 에 있는 문구)으로 — 하한 14 를 네 번째 자리에서 같은 뜻으로. 차트는 그대로. 결정은 기획. **안 고침.**
  - 별개로 적어만 둔다: 요일에는 n 임계가 없어 **오래된 노선에서도 n=1 짜리 요일이 「가장 쌉니다」가 될 수 있다.**
  - 그 7장은 sitemap 에서 빠져 있어 검색엔진에는 안 나간다(B64 T1) — 직접 방문·「다른 노선 보기」로만 보인다.
- ~~**남은 「30일」 셋**~~ → **해결**(`d726efc`).  기준: **`{D}` 는 「우리가 가진 표본」을 말하는 문장에만, 「30일 창으로 계산한다」는 방법 설명은 30 그대로.** `og:description`(D<14 판 포함)·메타 설명·항공사 리드 → `{D}`, 푸터 → 30 유지. 문구는 `COPY.md` 그대로. 바이트 대조: 바뀐 것 17장(얇은 7 + 30일 못 채운 10) · **30일 채운 26장·홈·sitemap 은 동일.**
- ~~**B67. 상세의 「평소 시세와 비교」가 없을 때 아무 말도 안 한다.**~~ → **해결**(`d70851c`·`76339db`, 2026-09-21). `median` 없음 → `아직 이 노선의 평소 시세를 모아두지 못했어요` · `median<=price`·반올림 0% → `지금은 평소 시세와 비슷해요`. 테스트 10개(고치기 전 코드에 돌리면 5개 빨개짐).
  - 스펙: `SPEC.md` §CH4 「막대가 없을 때 이유를 말한다」(2026-09-01 확정, **프론트가 지적해서 생긴 결정**이다). `median` = `null` → `아직 이 노선의 평소 시세를 모아두지 못했어요` ·
    `median` ≤ `price` → `지금은 평소 시세와 비슷해요`. **둘을 한 문구로 뭉치지 않는다.** `COPY.md` C-16 은 「해소」로 닫혀 있다.
  - 실제: `discover.js` `priceCompare()` 가 두 경우 **모두 `""`** 를 돌려준다. 두 문구는 코드에 **0건.** 막대가 뒤집혀 나오지는 않는다(생략될 뿐).
  - 규모(라이브 2026-09-21, 딜 138건): `median` ≤ `price` **65건(47%)** · `median` = `null` 5건(전부 `obs_days` 0) · 막대가 뜨는 것 68건. **상세 둘 중 하나는 근거 자리가 말없이 비어 있다.**
    라이브에서 열어 확인: `#PUS-FUK`(≤) 비교 구역 없음 · `#CJU-DAD`(null) 없음 · `#PUS-PQC`(대조군) `17%↓`·막대 83%.
  - 곁가지: `median` > `price` 인데 차이가 0.5% 미만이면 막대가 `0%↓` · 채움 100% 로 뜬다(3건 — `#SEL-MYJ` `발견가₩284,043 · 0%↓`). 「비슷해요」 쪽으로 보낼지는 기획 판단.
  - 안 고침. 백엔드 사실: `median` ≤ `price` 여도 `null` 이 아니라 값이 오고 `discount` 가 0 이다 — 프론트는 값을 직접 비교하므로 실해 없음.
  - ✅ **기획 답(2026-09-21 (2))**: 순서를 **B67 → B65** 로 바꾼다 — 「이미 있는 자리의 구멍이 새 길보다 먼저」. 반올림해 **`0%` 가 되는 것은 전부 `median <= price` 와 같은 처리**(「비슷해요」) — 화면에 0% 라고 쓰는 순간 그건 주장이 아니다. 사용자 승인 대기.
  - 🔒 기획이 `PLAN.md` **함정 13** 으로 못 박았다: **「✅ 해소」는 화면에서 본 뒤에만 적는다. 스펙 확정은 「확정」이지 「해소」가 아니다.**
    그리고 **전수 대조를 청했다(측정만)**: `SPEC.md`·`COPY.md` 에서 ✅ 로 닫힌 항목 중 화면에 없는 것을 센다 — 남은 것이 아니라 **닫힌 것**을 센다(함정 11). 지금 챕터와 B67 뒤에 따로 잡는다.
- ~~**B65. 딜의 `route` 필드를 프론트가 안 쓴다 — 상세에서 노선 페이지로 가는 링크가 없다.**~~ → **해결**(2026-09-21). 상세 맨 아래(가격 고지보다 아래)에 `이 노선 시세 자세히 →`, `route` 있을 때만(픽스처 40/138). 확인 중 버그 하나를 잡았다 — 새로 둔 `var BASE = ""` 가 **지도 배율 상수 `BASE = 200` 과 이름이 겹쳐** 링크가 `/200/routes/…`(404)로 나갔다. `node --check` 는 통과했고 **눌러 봐야 보이는 종류**다.
  - 🔴 **내 커밋 메시지의 한 줄이 과했다(2026-09-21 정정).** 「SEO 내부 링크도 막아 놓은 상태였다」고 적었는데 **크롤러는 보고 있었다** — 백엔드 SEO 실측 통지로 알고 직접 셌다:
    홈의 노선 링크 `<a>` **51개**(`<noscript>` 43 + 빈 날 화면 8)가 **정적 HTML** 이라 크롤러는 다 본다. `<noscript>` 는 JS 가 켜지면 안 그려지고 `.emptyday` 는 `hidden` 이라 **사람만 못 봤다.**
    B65 가 고친 것은 **사람의 길**이지 색인 경로가 아니다. `SPEC.md` IA-1 의 「SEO 내부 링크도 버리고 있었다」도 같은 과장으로 보인다 — 기획에 사실만 전했다.
  - ⚠️ **링크가 카드 접힘 아래다**(1440 173px · 1366×768 268px · 390 57px). 스펙이 정한 위치의 결과이고 「스크롤 없이 보이게」는 B61 때 지킬 수 없는 규칙으로 정리됐다 — 기획에 수치만 전했다. 백엔드는 「새 노선이 생기면 그 딜에 『이 노선 시세 자세히』 링크가 붙는다」고 알고 있는데(2026-09-20 통지),
  `discover.js` 에 `route` 를 읽는 곳이 **0곳**이다. 픽스처 딜 137건 중 35건이 `route` 를 달고 온다. B20(IA-1, 네비 `노선별`이 링크가 아님)과 같은 뿌리 — **JS 가 켜진 방문자는 홈에서 노선 페이지로 갈 길이 없다**(빈 날 화면과 `<noscript>` 뿐).
  **스펙은 이미 있다** — `SPEC.md` §CH6 「홈 → 노선 페이지 진입로」(2026-09-01 확정, IA-1): *확장 상세 맨 아래에 `이 노선 시세 자세히 →` 를 둔다.* `route` 가 있는 딜에만(파일 없는 `route` 0건). **확정됐는데 구현이 안 된 것**이다 — 다음 챕터 후보. 안 고침.
- **B56. `discover.css` 에 옛 헤더용 규칙이 남아 예산 칩에 샌다.** `.budget{display:flex;gap:7px;margin-left:4px…}`(:23)은 옛 헤더 예산 슬라이더 것인데, 지금은 `.fchip.budget` 칩이 그 클래스를 달고 있다.
  실측: 분위기 칩 `display:block · margin-left:0` ↔ 예산 칩 `display:flex · margin-left:4px · gap:7px` — 예산 칩만 간격이 다르다. 같이 죽은 것: `.budget b` · `.budget input[type=range]` · `.mood.pill`(:22) · `.gm-logo .muted`.
  (앞서 B49 에서 「`discover.css` 죽은 클래스 0/131」이라고 적은 건 **구별 못 하는 측정이었다** — 클래스 이름이 JS 어디든 나오면 산 걸로 쳤다. 선택자 200개를 12개 화면 상태의 실제 DOM 에 대 보니 위 5개가 나왔다. 나머지 28개는 그 상태에서만 안 나온 것 — 드래그 중·트윈 중·`noscript`·42%+ 도장 등.)
- **CH6 접근성에 넘길 실측**: 누르는 자리가 작다 — 첫 방문 띠 `×` **14×16px**, 상세 `×` 24×24, 정렬 알약 62×28, 필터 칩 높이 31, 단계바 29(권장 44). 글자도 작다 — 제휴 고지 `.hc-ad` 9px·`opacity .85`, 도장 T1 8.6px, 사진 위 태그 8.3px.
  제휴 고지 크기는 「고지이지 강조가 아니다」라는 기획 결정이지만 **법적 고지가 9px 인 건** 한 번 짚을 거리다. → **기획이 받았다**(2026-09-19): `DESIGN.md` 에 고지 문구의 크기·대비 하한을 정해 주기로 함(B54 보다 먼저 본다). **그때까지 지금 값 그대로 둔다.**
- ~~**B57. 제휴·가격 고지 글자가 기획 하한에 못 미친다.**~~ → **해결**(`fd832b8` 외, 2026-09-20). 라이브 실측: `(광고)` 10.24px·2.91:1 → **12px·4.61:1**, 설명줄 8.96px·3.48:1 → **12px·4.61:1**. `tests/test_disclosure.py` 7개가 잠근다(고치기 전 CSS 에 돌리면 4건 걸림). `DESIGN.md` §접근성 · `DECISIONS.md` 2026-09-20.
  - 기획 실측(흰 카드 위, 흐림 반영): `.cmp-ad`(`(광고)`) 10.2px · 대비 **2.91:1**, `.hc-ad`(설명줄·가격 신선도 고지) 9.0px · **3.48:1**. 기준은 WCAG AA 4.5:1. 원인은 `opacity` — `--sub` 자체는 4.61:1 이다.
  - 하한: **12px 이상(`.75rem`) · `--sub` 그대로 · `opacity` 없음 · 강조 없음**(코랄·굵게·배경칠 금지 — 중립성 결정 그대로). 위치는 지금 그대로.
  - 끝났다의 기준(기획 제안): 두 클래스의 계산된 대비 ≥ 4.5 · 글자 ≥ 12px 를 **테스트나 점검 스크립트로 잠근다**(흐림이 다시 끼면 잡히게). 설명줄이 길어지니 모바일 상세 줄바꿈을 스크린샷으로 본다.
  - 같은 패턴(작은 `--sub` + `opacity`)이 다른 곳에 있어도 **이번엔 고지만** — `.hc-seen`(9px · .75) · `.fr.stale`(.7) 등은 따로 적는다.
- **QA 목록(§3) 나머지를 2026-09-19 에 돌린 결과 — 이상 없음**: 900px · 제주 6곳/대구 6곳(희소 안내 정상) · 분위기 필터(79장 중 비매칭은 흐리게, 「전 지역에서 찾는 중」) · 매칭 0건 문구 ·
  상세 연 채 정렬/필터 변경(닫힘 — 스펙대로) · 모바일 도크 펼침·카드 탭 상세 · **딜 0건인 날**(`deals: []` 픽스처 사본으로 빌드 성공, 1440·390 모두 「오늘은 조용하네요」+ 노선 링크). 전 상태에서 가로 넘침 0 · JS 예외 0.
- ~~**README** — 로컬 빌드 예시 `--api https://galmal.kr/v1` 은 **404**(실측). 정답은 `https://api.galmal.kr/v1`.~~ → 같은 날 README 개편에서 고쳤다.
- 다른 저장소(사실만 전달함, 안 고침): `galmal-backend/README.md`·`galmal-plan/README.md` 가 옛 `promo-ticket-site` 프로토타입 설명 그대로다(두 파일 내용 동일, 41줄).

### 모바일 — 실기기 첫 확인 (2026-09-20)

- ~~**B68. 「최근 1일간 …」 — 얇은 노선의 항공사 리드.**~~ → **해결**(`a393c17`). `D`<14 면 `지금까지 이 노선에서 수집된 …`(기획 확정) — hero 의 `지금까지 최저가` 와 같은 말. `COPY.md` 항공사 리드는 `최근 {D}일간 …` 한 판뿐이라 D=1 이면 이렇게 나온다.
  틀린 말은 아니다(그날 수집한 게 맞다). 머리말·공유 문구처럼 `D` < 14 판이 필요한지는 기획 몫 — **문구를 지어내지 않고 그대로 뒀다.**
- **B62. 🔴 사용자가 폰으로 처음 열어 봤다 — 「모바일은 나중에 크게 한번 손봐야 될 듯」.** 이 저장소의 모바일 확인은 그동안 **전부 헤드리스 크롬(CDP 390px)**이었고(§0-5), 헤드리스로는 넘침 0 · 예외 0 으로 「이상 없음」이 나왔다.
  **재는 자가 못 보는 문제가 있다는 뜻이다** — 터치·시트 끌기·글자 크기 체감·브라우저 주소창·기기 폭 같은 것.
  - **받은 증상 ①(2026-09-20, 사용자가 다른 사람에게 물어봄 — 원문 그대로)**: 「위 아래 스크롤이 딱 명확하지가 않음」 · 「내가 의도한 건 아래로 내려가야 하는데 안 내려가고」.
    기기·브라우저·어느 자리를 밀었는지는 모른다. **아래는 코드에서 읽은 가설이지 확인한 원인이 아니다** — 실기기에서 어느 것인지 가려야 한다:
    (a) 페이지 자체는 스크롤이 없다(`html,body{height:100%}` + 지도가 화면을 채움). **지도 위를 밀면 아무 일도 안 일어난다.** 보통 웹페이지처럼 「아래로 내리면 더 나온다」를 기대했다면 그게 이 증상이다.
    (b) 카드 목록은 **시트 안(기본 340px)에서만** 스크롤된다. 시트 머리(`오늘의 발견`·정렬 줄)는 `touch-action:none` 인 **끌기 손잡이**라, 거기서 아래로 밀면 목록이 내려가는 게 아니라 **시트가 줄어든다.**
    (c) 「더 볼 수 있다」는 신호가 약하다 — 손잡이 막대(38×4px) 하나뿐이고, 시트를 위로 키울 수 있다는 걸 알려 주는 게 없다.
    → 어느 쪽이든 **시트 모델 자체의 문제**라 작은 수정이 아니다. 기획(SPEC §CH4·§CH5)과 모바일 챕터로.
  - **관찰 ③(2026-09-21, 사용자 본인 · iPhone 14 Pro · Safari)**: 손잡이 탭 「다 됨」. **고친 뒤에도 남는 답답함은 따로 말하지 않았다** — ①②(지도 둘러보기·지도 크기)의 몫은 아직 안 재봤다.
  - **받은 증상 ②(2026-09-20, 사용자 본인 · 삼성 폰 — 원문 그대로)**: 「모바일에서 사용하면 좀 답답해. 예를 들어 지도를 좀 둘러보려고 했는데 안돼. 그리고 지도가 너무 작게 보이고,
    오늘의 발견 딜을 더 많이 보려고 위에 바를 누른거 같은데 지도가 눌려서 아무 반응이 없고 아니면 아래가 눌려 그리고 딜을 누르고 자세히 보기만 누르는게 아니라 이렇게 아래 창을 위로 올리면 확장되도 좋을거 같아」
    (증상 ①의 기기도 「그냥 삼성 폰이었을 거야」.) 넷으로 갈린다 — **앞의 셋은 관찰, 넷째는 제안**이다:
    1. **지도를 둘러볼 수 없다.** 끌어도·벌려도 안 움직인다. 이건 버그가 아니라 **결정**이다 — 자유 줌·팬을 안 넣기로 했고(SPEC §CH1), 모바일 시트 끌기가 그 결정에 기대고 있다(`discover.js` 시트 주석 「자유 줌을 넣었다면 이 설계가 성립하지 않았다」). **사용자 기대와 정면으로 부딪힌다.**
    2. **지도가 너무 작다.** 실측(라이브, 헤드리스): 360×780 에서 지도 **351px = 화면의 45%**, 그 위에 접힌 도크·단계바가 얹혀 **빈 지도는 255px**. 412×915 는 486px(53%)·390px. 실제 폰은 브라우저 주소창만큼 더 짧다.
    3. **시트를 키우려고 「위의 바」를 눌렀는데 안 먹는다 — 지도가 눌리거나 아래 카드가 눌린다.** 실측: 손잡이를 **탭**하면 시트 340 → 340, **아무 일도 없다**(끌기만 된다). 손잡이 막대는 38×4px,
       잡히는 자리는 `오늘의 발견` 줄 ~~36px~~ **44px 높이**(정정 — 아래 ③ 해결 줄)이고(그 아래 정렬 알약 줄은 끌기에서 빠진다) 바로 위는 지도, 바로 아래는 카드다. 기본 시트(340px)에 **온전히 보이는 카드는 1장.**
    4. (제안) **카드 → 「자세히 보기」를 누르는 것 말고, 아래 창을 위로 올리면 확장되면 좋겠다.**
    → 증상 ①의 「내리려는데 안 내려간다」도 3번과 같은 자리로 보인다(가설). 관찰 **3건**(삼성 2 · iPhone 1).
  - ✅✅ **③ 실기기 확인 완료 — 닫힘(2026-09-21).** 사용자가 **iPhone 14 Pro · Safari** 로 직접 눌러 보고 「다 됨」. 다섯 가지 전부(탭으로 커짐 · 다시 눌러 줄어듦 · 글자 옆·정렬 줄 빈 자리도 먹음 · 정렬 버튼은 시트를 안 움직임 · 끌기 그대로).
    이 저장소 **첫 iPhone 관찰**이고, 헤드리스 터치로만 확인했던 것이 실기기에서 맞았다. 기획 착수 조건(관찰 3건)의 세 번째.
  - ✅ **③ 해결(`2a01f49` 외, 2026-09-20 · 사용자 승인).** 라이브 실측(진짜 터치 이벤트 · 360×780): 손잡이 탭 ×4 `340→340→340→340` → **`340→619→340→619`**, 접힌 단에서 탭 160→340, 끌기·정렬 알약·카드 탭은 그대로. 막대 38×4 `--line` → 48×5 `--sub`, 잡으면 머리가 눌린 색.
    ⚠️ **앞서 적은 「잡히는 자리 36px」은 틀렸다 — 44px 이다**(정렬 줄 위 여백 8px 을 빼먹고 셌다). 좁았던 것보다 **탭이 안 먹고 막대가 안 보인 것**이 본 원인. 지금은 48px.
    ⚠️ **헤드리스 터치로 확인한 것이다.** 실제 손가락은 사용자가 폰에서 눌러 봐야 한다 — 그게 관찰 1건이기도 하다(고친 뒤에도 남는 답답함이 ①②의 몫).
  - ✅ **기획 판정(2026-09-20 (4), `DECISIONS.md` · `SPEC.md` §3) — ③만 떼어 지금 고친다.** 가르는 기준: **「어느 시트 모델을 택해도 살아남는가」** — 「눌렀는데 아무 일도 없다」가 옳은 안은 없다.
    ⓐ 손잡이를 **탭하면 한 단 커진다**, 이미 확장이면 한 단 줄어든다(끌기는 그대로) ⓑ **시트 머리 전체**가 잡는 자리 · 세로 **44px 이상**(정렬 알약 같은 실제 컨트롤은 제외) ⓒ **잡는 자리가 눈에 보이게** — 새 문자열이 필요하면 먼저 기획에.
    **하지 않는다**: 시트 기본 높이 · 지도 비중 · 상세 여는 방식. ①(둘러보기 — 2026-08-22 결정, 지금 안 뒤집는다 · PH 에서 「자유 줌 아닌 것으로 갚는다」부터) · ②(비중이 아니라 나눠 갖는 방식) · ④(§CH4 와 함께)는 재검토 PH.
    관찰 3건째는 **다른 사람 또는 다른 기기**(특히 iPhone Safari)로. ③을 고친 뒤 같은 사람에게 다시 보여 주는 것도 1건이다.
  - ✅ **기획이 받았다(2026-09-20, `SPEC.md` §3 🔴).** PH5(도크 → 하단 시트)와 묶어 **모바일 재검토 PH 하나**로 간다. **작게 고치지 않는다.**
    착수 조건: **실기기 관찰 3건 이상** — 기기·브라우저 / 어느 자리를 밀었나 / 무엇을 기대했나. 지금 **3건**(①은 자리 미상 · ② 네 가지 · ③ iPhone 확인). **착수 조건을 채웠다.** 위 가설 셋은 관찰할 때 「무엇을 볼지」의 목록으로 쓴다.
    증상은 여기에 **원문 그대로** 쌓는다.
  - ⚠️ **증상은 하나만 받았다.** 추측으로 고치지 않는다 — 증상(어느 화면·무슨 동작·기기/브라우저)을 받아 적은 뒤, 기획과 모바일 챕터(CH5 후속)로 묶는다.
  - 같이 볼 기존 항목: B10(핀 탭) · B16(도크가 핀을 가림, PH5 하단 시트) · CH6 메모(누르는 자리 14×16·24×24px, 8~9px 글자) · B36 주석(「실기기에서 다시 봐야 한다」).

- **B69. 🔴 막대가 적은 차트가 폭 전체에 흩뿌려져 빈 판처럼 보인다 — 사용자 지적(2026-09-21, 모바일).** 「인천 → 타이중 … 출발 월별 최저가에서 막대 꼴랑 2개 있으니까 좀 별론데」
  - **데이터 문제가 아니다.** `ICN-RMQ` 는 월 버킷이 실제로 2개다(10월 n=14 · 12월 n=6). 2개는 이 프로젝트 규칙상 **비교가 성립하는 수**다(`MIN_BUCKETS=2` 「하나는 비교가 아니다」).
  - **배치 문제다.** `charts.py bar_chart()` 가 `slot = 폭/막대수` 로 칸을 나누고 막대를 그 한가운데 둔다 — 막대 수가 적으면 **칸이 거대해져 막대가 멀리 떨어진다.**
    실측(라이브 390px): 차트 폭 322px · 막대 11px 두 개가 x=77 과 x=234, **사이 간격 146px.** 1440px 에서는 간격 **436px**. 막대 9개인 `ICN-FUK` 은 간격 24px 로 정상으로 보인다.
  - `DESIGN.md` dataviz 규격은 **「막대 ≤24px · 2px 간격」**이다 — 지금 렌더는 간격 쪽에서 그 규격을 벗어난다. 막대 수가 많을 때만 맞는 구현이었다.
  - 같은 자리에 걸리는 노선(라이브 43): 막대 2개 **3개 노선**(`ICN-RMQ`·`GMP-NGO`·`ICN-PQC`) · 3개 2노선 · 4개 1노선. 요일 차트도 같은 함수라 `ICN-RMQ` 는 5칸이 퍼져 있다.
  - 고칠 방향(프론트 제안): 칸 너비에 상한을 두고 **막대 무리를 가운데로 모은다**. 막대 수가 많을 때는 지금과 같다(상한에 안 걸림). **시각 결정이라 기획 확인 뒤.** 안 고침.

- ~~**B70. 🔴 필터가 켜진 동안 지도를 다시 가까이 볼 방법이 없다.**~~ → **해결**(`60f491d` 외, 2026-09-22). 뷰가 **언제나** 거리 단계를 따르고, 「아주 멀리로」는 필터를 **켜는 순간 한 번**뿐이며 끌 때 되돌리지 않는다. 필터 중에도 단계 버튼이 보인다. 라이브 실측(PC): 날짜 칩 켬 배율 0.85 → `－가까이` 3.32 → 또 6.92, 불과 지도가 전부 일치.
  - 🔴 **고치는 중 새 결함을 만들었다가 잡았다**: 단계바 불을 `setStage()`·`applyHash()` **두 곳**에서 켜고 있어, `applyFilter()` 가 `stageIdx` 를 옮기는 새 길이 생기자 **불은 「가까운 곳」·지도는 「아주 멀리」**로 어긋났다. `syncStageBar()` 한 곳으로 모았다 — 테스트가 「불 켜는 곳은 하나」를 잠근다.
- **B70. 🔴 필터가 켜진 동안 지도를 다시 가까이 볼 방법이 없다 — 사용자가 실사용 중 발견(2026-09-22).** 「날짜를 지정하면 지도가 줌아웃 되는데 **다시 가까이 갈 방법이 없어**」
  - 원인: `render()` 의 `viewOf(anyFilter() ? "far" : STAGES[stageIdx])` — 필터가 하나라도 켜져 있으면 뷰를 **아주 멀리에 잠근다.** `setStage()`·스테퍼는 `stageIdx` 를 바꾸고 단계바 불도 켜지는데 `render()` 가 그 값을 안 본다. **버튼은 정상 동작하고 화면만 안 따라온다** — 테스트가 못 잡는 종류다.
  - 🔴 **기획이 못 본 것 하나 더**(내가 재현하다 찾음): 필터를 켜면 CSS `.stagebar.allregions .pill{display:none}` 로 **단계 버튼 3개가 화면에서 사라진다.** 남는 건 스테퍼뿐인데 그 시점 `stageIdx` 가 0 이라 **`－가까이` 는 잠겨 있다.** 누를 수 있는 건 `＋더 멀리` 하나이고 그건 화면을 안 움직인다 — **가까이 갈 길이 정말로 하나도 없다.**
  - 실측(라이브 1440): 필터 없음 핀 24·단계바 3개 보임 → 날짜 칩 켬 핀 79·단계바 **0개 보임**·`－가까이` 잠김 → `＋더 멀리` 누름: 내부 단계만 「조금 더 멀리」로 바뀌고 **핀 79 그대로** → 필터 끔: 핀 24 로 정상 복귀.
  - 스펙 원인은 기획이 인정했다(§CH2 「필터를 켜면 아주 멀리로 자동 전환」을 「한 번 옮긴다」와 「켜진 동안 잠근다」로 안 갈랐다). 고칠 것은 `SPEC.md` §CH1 에 있다: **켠 순간 한 번 이동 → 그 뒤 조작은 전부 먹는다 → 꺼도 되돌리지 않는다.**
  - 사용자 승인 대기.
- **인원 선택 — 스펙 예고, 착수 금지(2026-09-22).** 사용자: 「몇 명이서 가는지가 안 정해져 있다」. 결정: 가격 단위를 `왕복` → **`1인 왕복`**, 확장 상세에 **`몇 명이 가요?` 1~4명 칩**(기본 1, 세션 안에서만 기억), 고르면 **예약처 링크의 인원 값만** 바뀌고 화면 가격·도장·막대는 그대로 1인. **`×2` 총액은 안 만든다.** 고지 한 줄은 가격 고지 **바로 위**(사슬 밖).
  🔒 **계약 `links[].pax` 가 실린 뒤에 시작한다** — 어느 예약처가 URL 로 인원을 받는지는 **링크를 만드는 백엔드만 안다.** 이름으로 추측하면 `ad` 때의 추측 넷이 다시 생긴다. 기획이 계약 반영 뒤 통지하기로 했다.

- **B71. 첫 화면이 355KB 를 통째로 나른다 — 절반이 「나중에야 쓰는 것」이다(2026-09-22 실측, 안 고침).** 백엔드가 인원 계약 형태를 물어와 재 봤다.
  | | 바이트 | 비중 |
  |---|---|---|
  | 홈 `index.html` | **354,913** | |
  | └ `__WORLD` 지도 윤곽 | 165,395 | **47%** |
  | └ `__DEALS` 인라인 딜 | 174,437 | 49% |
  | └ 그중 `links[].url` 690개 | 73,539 | 딜의 42% |
  - `links` 는 **확장 상세를 열 때만** 필요하고, 지도 윤곽은 **PH5b 뒤 모바일 첫 화면에서 빠질 수 있다**(기본이 피드가 되면).
  - 둘 다 인라인인 이유는 `file://` 로도 열리게 하려던 것(`site/build.py` 주석). 그 이유가 지금도 유효한지부터 물어야 한다.
  - 🔒 **PH5b 결론 뒤에 한 챕터로.** 지금 나누면 두 번 나눈다. 인원 계약은 이걸 기다릴 필요 없다 — B 안이면 +73KB 라 지금 감당된다.
  - 프론트 의견을 백엔드에 보냈다: **B(치환 토큰 한 벌)** — A(인원별 완성 URL 4벌)는 **같은 사실이 네 곳**이 되고, 예약처가 파라미터를 바꾸는 날 몇 벌만 고쳐지는 길이 열린다(어휘 손 사본·계약 사본과 같은 모양).

### ✅ 전수 대조 (2026-09-22, 기획 요청) — 「닫혔다고 적힌 것」 중 화면에 없는 것

> 왜: B65(노선 링크)·B67(비교 막대 문구)·B70(필터 중 단계)이 전부 **「✅ 확정」으로 닫힌 뒤 아무도 화면을 다시 안 본** 것이었다.
> 셋 다 우연히·사용자가 찾았다. 기획이 `PLAN.md` **함정 13**(「✅ 해소」는 화면에서 본 뒤에만 적는다)을 세우고 전수 대조를 청했다.
> **측정만 했다 — 아래 다섯은 고치지 않았다.**

**어떻게 셌나**: `SPEC.md` 의 ✅/취소선 줄 **57개** + `COPY.md` 의 문장형 확정 문구 **112개**를 뽑아, 구운 산출물(HTML 43+1장)·
`discover.js`·`discover.css`·`site/*.py` 에서 찾았다. HTML 태그를 걷어내고 자리표시자(`{노선}`)를 건너뛰며 대조했다.
COPY 쪽 「없음」 61개 중 **대부분은 남의 몫이거나 정상**이다 — 메일 문구 약 24개(백엔드 BE6) · 폐기된 옛 판(화면0 문구·`사흘 전에 본 가격이에요`) ·
아직 안 만들기로 한 것(인원 선택·PH5b `지도로 보기`) · 금지어 예시(`최저가 갱신`) · 동적 생성이라 문자열이 없는 것(`서울 출발 · 65곳`).
**판정은 내가 눈으로 했다** — 그 분류는 셈이 아니라 판단이다.

**진짜 빈 것 다섯**(전부 ✅ 로 닫혀 있다):

| 스펙 | 무엇을 하기로 했나 | 화면 실측 |
|---|---|---|
| ~~§CH3 `SPEC:768`·`784` **신기록**(2026-09-01 확정)~~ → **해결**(2026-09-22) | 카드는 `{N}일 중 최저`, 확장 상세는 도장과 나란히 `{N}일 중 가장 싼 가격이에요` | ✅ CDP 실측 신기록 카드 3장(칭다오 19·나트랑 17·두바이 19) · 도장과 겹친 카드 **0장** · 바르셀로나 상세에 둘 다. 돌연변이 11개 전부 빨강 |
| §CH6 `SPEC:1055` **키보드·스크린리더**(B11) | 지도 `aria-label="서울 출발 69곳"` · 개별 핀 `aria-hidden` · 키보드 조작표 | aria-label 이 **고정 문구** `여행지 발견 지도` · 핀 `aria-hidden` **0/23** · `tabindex` **0개** |
| ~~§CH6 `SPEC:1115` **갱신 시각 노출**(C-15)~~ → **해결**(2026-09-22) | 피드 헤드 뒤에 `· 07:23 기준`, 낡으면 절대 날짜 + `어제/N일 전 자료예요` | ✅ 셋 다 화면에 있다. **KST 로 판정**한다 — 로컬 날짜였으면 해외에서 경고가 사라진다(테스트에 대조군) |
| §CH3 `SPEC:1125` **`서울`은 인천+김포** | 확장 상세 가격 줄 아래에 **실제 출발 공항**(`인천 출발`) | 상세 본문에 `인천 출발`·`김포 출발` **없음** |
| §CH4 `SPEC:1236` **패널 폭 `380px`**(「바꾸지 않기로 정했다」) | 380 | 실측 **340** — 40px 차이 |

**문서끼리 어긋난 것 둘** — 둘 다 신기록에서 나왔고 **둘 다 기획이 정리했다**(`galmal-plan` `effb930`, 2026-09-22):
① 관측 하한이 `SPEC:782` **`>= 14`** 인데 `COPY.md:184` 는 **`≥7`** 이었다.
(내 백로그에도 옛 `>=7` 이 남아 있어 09-20 에 기획에 잘못 전한 적이 있다 — **수치는 백로그가 아니라 SPEC 에서 본다.**)
② 카드에 보일 글자가 `SPEC:790` **`{N}일 중 최저`** 인데 `COPY:184` 는 **`{N}일 중 가장 싼 가격이에요`** 였다.
착수하며 발견해 **코드를 쓰기 전에** 물었다 — 문구는 `COPY.md` 가 정본이라 그대로 썼으면 도시명 줄에 14자가 들어갔다(그 자리의 최장은 9자다).
🔴 **이게 전수 대조가 못 보는 세 번째 종류다.** 스펙과 문구가 **서로** 어긋나면 어느 쪽을 찾아도 「있다」로 세어진다 —
화면에 없는 것(다섯)도, 동작으로만 정해진 것도 아닌, **문서끼리 갈린 것**이다. 구현할 때 둘을 같이 펴 봐야 나온다.

**대조에서 뺀 것**: §CH5 모바일 시트 관련 ✅(기획 지시 — PH5b 로 곧 사라질 화면).
**이 대조가 못 보는 것**: 문구가 아니라 **동작**으로만 확정된 것(예: 「같은 것을 다시 누르면 닫힌다」)은 문자열 검색으로 안 잡힌다.
그런 것은 화면에서 눌러 봐야 한다 — B70 이 그 종류였고 **사용자가 찾았다.**

### 🔴 PH5b — 모바일 재검토가 열렸다 (기획, 2026-09-21)

사용자가 iPhone 으로 써 본 뒤: **「지도가 너무 작아서 그냥 딜만 보여주는 걸로 갈까 모바일은? 거의 쓸모가 없음」**
B62 ①②(둘러볼 수 없다·작다)의 답이 「지도를 고친다」가 아니라 **「기본 화면에서 뺀다」**로 왔다. `DECISIONS.md` 2026-09-21 (6).

- **아직 결정 아니다.** 기획 권고는 「기본은 피드, 지도는 버튼 뒤 전체 화면」이고, 사용자가 **폰에서 직접 여는 목업**(A 피드만 / B 피드+지도 버튼)을 보고 고른다.
- 근거가 된 것은 **내가 잰 숫자**다 — 지도 45% · 빈 지도 255px · 온전히 보이는 카드 1장(「지도도 목록도 반쪽」). 그리고 2026-09-01 의 탭 전환 기각은 **컴퓨터에 그린 390px 틀**을 보고 내린 판단이라 실기기에선 근거가 무너졌다고 기획이 적었다.
- 🔒 **모바일 시트 쪽에 새로 공들이지 않는다.** 결정이 나면 CH5(시트)가 통째로 바뀔 수 있다. 시트·핀 위 카드에 묶인 것(B59·B60)은 PH5b 뒤로.
- 살아남는 것: **확장 상세 «안»의 내용**(B67 비교 문구·B65 노선 링크·B61 고지 순서)은 어느 안에서도 그대로다. 노선 페이지(B69 등)도 무관하다. 데스크톱은 그대로 간다.

### SEO 실측 (2026-09-21, 백엔드가 서치콘솔로 잼 — 프론트 조치 불필요)

- **프론트 기술 항목은 전부 정상.** `robots.txt`·`sitemap.xml` 37건(`lastmod`·`changefreq`·`priority`) · canonical 8표본 전부 자기 참조 · 제목이 노선마다 다름 · 홈의 정적 노선 링크 51개 · 본문 1,431~2,315자. **고칠 것 없음.**
- 색인된 페이지가 **대부분 아직 안 됐다.** 이유는 전부 「발견됨 - 현재 색인이 생성되지 않음」 하나 — **품질 판정이 아니라 아직 크롤링을 안 한 것이다.** 백엔드도 처음엔 「본문이 얇아 거부됐다」고 의심했다가 틀린 것으로 확인했다.
- 검색에 노출되는 노선은 **아주 소수**이고 나머지는 0이다.
- ⚠️ **모바일 순위가 데스크톱보다 눈에 띄게 낮다.** 표본이 작아 결론짓기 이르다 — 색인이 늘면 다시 볼 값. **B62 모바일 재검토의 입력이 될 수 있다**(모바일 화면이 나쁘면 순위에도 나타난다).
- 백엔드는 이 실측으로 **노선 추가(BE8 2차)를 보류**했다 — 대부분이 노출 0인데 비슷한 페이지를 더 만들 이유가 없다. 노선은 당분간 43개.

> 🔒 **수치는 여기 적지 않는다**(`SESSIONS.md`, 2026-09-21). 검색 노출·클릭·순위·방문자는 「우리에게 사람이 얼마나 오는가」라 **노션**에 둔다.
> 2026-09-21 에 내가 이 절에 노출·클릭·순위 숫자를 적었다가 내렸다 — **이력에는 남는다**(force push 안 함).
> 적어도 되는 것은 수집·발행 수치(딜 건수·노선 수·표본 수·가격)다. API 가 이미 공개하고 있다.

## 다른 세션에 전달할 것

전달 완료·처리된 항목은 여기서 지운다. 남은 것만 둔다.

- **기획 세션** — 전달한 7건(B14~B17 + 50m 오기 + B3·B10) **전부 반영 완료**(2026-08-22, PH0).
  `DESIGN.md`는 시각 언어만 남기고 `SPEC.md`·`COPY.md`·`IA.md`·`FLOWS.md`로 분리됐다.
  **CH1 착수는 `SPEC.md` §CH1의 미결(특히 B14 택1)이 닫힌 뒤** — 기획 PH1 결과 대기 중.
- **백엔드 세션** — `seen` 필드 요청 **반영 완료**(`CONTRACT.md` §`seen`).
  남은 것: 현 픽스처에 `seen`이 아직 없다. 다음 크론 산출물부터 들어올 것으로 보이며, 프론트는 `null`/부재 모두 처리한다.

## 해결됨

- ~~**B0. 필터를 안 켰는데도 100만원 초과 딜 8건이 흐리게 표시된다.**~~ → CH0에서 해결. 예산 조건을 슬라이더가 실제로 켜졌을 때만 적용하도록 수정.
