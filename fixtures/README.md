# fixtures/ — v1 API 응답 사본 (로컬 개발용)

> 🔴 **계약의 정본은 `CONTRACT.md` §v1 이다. 이 폴더는 사본이다.**
> **손으로 고치지 말 것.** 고쳐야 할 것 같으면 계약이 틀린 것이고, 그건 기획에 올린다.

## 왜 있나

분리 후 프론트 저장소는 **네트워크 없이도 빌드할 수 있어야** 한다.
백엔드가 발행하기를 기다리지 않고 화면을 짜기 위한 설비이며, 이전용 임시방편이 아니다.

## 규칙 — 받아 적는 것이지 손으로 쓰는 것이 아니다

| | |
|---|---|
| 만드는 법 | `python fixtures/capture.py <커밋 또는 URL>` — **한 단계** |
| 계약이 바뀌면 | **다시 받아 적는다.** 손으로 맞추는 순간 「계약이 정본」이 깨진다 |
| 배포에 쓰나 | **안 쓴다.** 배포는 언제나 실물 API를 본다 |

낡아도 **배포는 안 틀리고 로컬만 낡는다.** 모든 응답에 `generated`가 있어 낡으면 눈에 보인다.

JSON 에는 주석을 못 다니 이 경고가 파일 안에 못 들어간다 — 그래서 여기 둔다.
`capture.py` 가 받아 적을 때마다 아래 출처를 다시 쓴다.

## 지금 사본의 출처

```
출처    https://api.galmal.kr/v1  (라이브)
발행    generated 2026-09-21T07:23:55+09:00 · preserved false
받은 것 47개 — meta · deals · vocab · routes/index · routes/{code} 43
확인    capture.py 가 받아 적은 직후 빌드와 같은 스냅숏 규칙으로 검사 — 한 발행분 ✅
```

🔴 **이 사본에는 「얇은 노선」이 들어 있다.** 백엔드가 노선을 36 → 43 으로 늘린 다음 날 받아서, 새 7개
(`PUS-BKK`·`PUS-HAN`·`TAE-CJU`·`GMP-SHA`·`GMP-TPE`·`GMP-NGO`·`ICN-PQC`)가 **수집 1일째**다.
그래서 같은 응답 안에서 **두꺼운 36 + 얇은 7** 두 경로가 같이 검사된다 — 얇은 쪽(sitemap 제외 · `수집 N일째`
머리말 · 「평소 시세」 칸 없음)은 그 전 픽스처로는 **실데이터로 한 번도 안 지나갔다.**
다시 받을 때 **두 갈래가 다 남아 있는지** 본다(`tests/test_route.py` `test_fixture_has_both_kinds`).

🔴 **신기록 딜도 두 갈래가 다 있어야 한다.** `low`·`obs_days≥14`·낙폭 5%+ 를 넘는 딜 중
**도장(15%+)과 겹치는 것**과 **안 겹치는 것**이 둘 다 있어야 「카드엔 하나만, 상세엔 둘 다」를 실제로 재 볼 수 있다.
지금 사본에는 겹치는 것 1건(바르셀로나 17%↓·19일)·안 겹치는 것 3건이 있다.
다시 받을 때 한쪽이 0건이 되면 `tests/test_record.py` `test_fixture_has_both_kinds` 가 **실패한다** —
검사가 눈을 감은 채 초록불이 되는 것보다 낫다.

**이 사본은 한 발행분이다.** 그래서 픽스처 빌드도 라이브 빌드와 **같은 검사**를 받는다 —
예전엔 `--no-snapshot-check` 로 검사를 껐다.

### 이전 사본 (2026-09-08 ~ 2026-09-19)

`05d0de9`(M1) 의 `docs/v1/` 39개였다. M2 동등성 증명(「같은 입력 → 같은 출력」 39/39)의
**입력**이라 그 커밋의 산출물과 짝으로 묶어 두었다. 그런데 「한 발행의 응답은 같은 `generated`」
규칙(2026-09-17)보다 먼저 만들어져 **`deals` 만 42초 일렀고**(`15:28:00` vs `meta 15:28:42`),
그래서 픽스처 빌드에만 검사를 꺼야 했다(BACKLOG B40).

2026-09-19 `vocab.json` 이 생기며 새로 받았다. 그 한 파일만 옛 사본에 끼우면 **날짜가 섞인 사본**이
되는데, 검사가 꺼져 있어 아무도 모르고 통과했을 것이다. 그래서 40개를 한 발행으로 다시 받았다.
M2 증명은 끝났으므로 짝을 유지할 이유는 없다 — 증명 기록은 `galmal-plan` `SPLIT.md` 이력에 있다.
